---
name: agent-package
description: "`.agent` 任务包的封装、加密、签名、版本、重放检测、冲突处理与原子导入；按需加载，协议变更必须同步 schema 与兼容性测试。"
compatibility: opencode
metadata:
  project: coevo
  scope: mvp
---

## 权威内容（单一权威源）

本技能内容与 Codex 侧 `.agents/skills/agent-package/SKILL.md` 同源。
执行相关任务前**先读取**该文件——实现守则、协议变更守则均以
`.agents/skills/agent-package/SKILL.md` 为准；若该文件缺失或不可读，
停轮上报，不得凭记忆执行。
