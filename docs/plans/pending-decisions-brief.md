# 待决策事项简报（MATURITY，2026-08-12）

> 用途：把剩余两项业务决策浓缩成"决策卡"，业务负责人逐项裁决并在
> `loop/DECISIONS.md` 留痕即可；决策结果将同步更新 `external-gates.md`、
> `capability-status.md`、README 与计划文档。本简报**不替代**裁决本身。

---

## 决策卡 1：O-01 在线/受控网络协同版本边界

**现状**：MVP 仅验证离线闭环；`sync/` 的离线文件式对账已 E2E 验证
（`INTEGRATION_VERIFIED`）；受控网络传输与中心端聚合为 `DESIGNED`
（`online-mode-scope.md`、ARCH-REVIEW-13 声明纪律）。

**选项**：

- **A（推荐）**：维持"离线闭环 = 当前版本范围；受控网络/在线协同 = 下一版本范围"。
  立即生效、无新增范围、与 ARCH-REVIEW-13 一致；O-07 在线半随之下调为后续版本项。
- B：把受控网络协同纳入当前路线图，接受双节点在线同步实现与运维范围扩大
  （需新增传输层设计 + 安全审查，且与 R-02 密码产品审批联动）。
- C：正式立项"在线模式"为独立版本（v0.5），给出单独时间盒与预算。

**裁决动作**：选 A → 在 DECISIONS 记录并把 O-01/O-07 在线半状态落定；
选 B/C → 生成对应的切片计划（规划阶段另开）。

---

## 决策卡 2：R-05 AI 定位正式裁决

**现状**：T-01 已于 2026-08-12T12:00:00Z 定稿**选项 B（确定性任务编排 + 可选模型
辅助）**，标注"可修订"；README/能力矩阵已按该口径落地；模型适配层保留为选项 A
（本地模型接入）的扩展点。

**选项**：

- **B（确认当前定稿）**：产品定位为确定性编排 + 可选模型辅助；O-08 的模型推断
  风险建议作为可选增强保留。
- A：升级为本地模型接入主链路（任务分解/风险建议默认走模型，离线回退兜底），
  需增加本地推理环境依赖与运维文档。

**裁决动作**：确认 B → DECISIONS 补"业务负责人确认"记录；改 A → 把 T-01 修订为
选项 A 并触发 T-02 正式实现。

---

## 裁决后自动同步点

| 决策 | 生效后更新 |
|---|---|
| O-01 | `external-gates.md`（如需新增行）、`capability-status.md` 同步行、`sync-protocol.md` 状态、计划文档 O-01/O-07 |
| R-05 | `loop/DECISIONS.md`（确认/修订）、README 定位句、`capability-status.md` US-2/US-11 注记 |
