# Requirements ↔ Code ↔ Test Matrix

| 故事 | AC | 行为简述 | 代码入口 | 测试文件 | 门禁结果 | 状态 | 最近变更 commit |
|---|---|---|---|---|---|---|---|
| ENG-BASE | AC-1 | 失败关闭、离线工程底座与签名审计链头 | `scripts/validate_opencode.py`; `scripts/quality_gate.py`; `scripts/loop_state.py`; `scripts/audit_log.py`; `scripts/audit_seal.py`; `scripts/audit_signature.ps1`; `.opencode/tools/`; `.opencode/plugins/path-policy.mjs` | `tests/unit/test_engineering_baseline.py`; `tests/unit/test_traceability_check.py`; `tests/integration/test_tool_contracts.py`; `tests/security/test_audit_log.py`; `tests/security/test_audit_seal.py`; `tests/security/test_loop_state_transaction.py`; `tests/security/path_policy_test.mjs`; `tests/e2e/test_offline_baseline.py` | pass `31c1e373bc9aad53`; verifier pass; security pass | done | `87b1e99` |
| ENG-LOOP-ENV | AC-1 | 锁版本、仓库本地、无运行时下载的 OpenCode/Make Loop 环境 + permission.bash 白名单可验证 | `docs/dependencies/toolchain-lock.json`; `scripts/enter-dev-environment.ps1`; `scripts/dev.ps1`; `scripts/tool-shims/make.cs`; `scripts/validate_opencode.py`; `opencode.jsonc` | `tests/unit/test_dev_environment_tools.py`; `tests/unit/test_permission_whitelist.py`; `tests/integration/test_dev_environment_entry.py`; `tests/security/test_local_toolchain_security.py`; `tests/e2e/test_loop_environment.py` | pass `e050cf72f6cda47e`; verifier pass; security pass `1458f00e53463d6f` | done | — |
| US-0 | AC-1 | 建立用户 / 客户端 / 证书数据模型 | `src/coevo/identity/models.py`; `src/coevo/identity/certificates.py`; `src/coevo/identity/validation.py`; `src/coevo/identity/audit_anchor.py`; `src/coevo/identity/repository.py`; `src/coevo/identity/service.py`; `src/coevo/identity/schema.sql`; `scripts/inspect_certificate.ps1`; `scripts/identity_freshness.ps1` | `tests/unit/test_identity_validation.py`; `tests/integration/identity_store_test.py`; `tests/security/test_identity_store_security.py`; `tests/security/test_identity_freshness_security.py`; `tests/security/test_identity_retirement_security.py`; `tests/e2e/test_identity_dev_environment.py` | quality pass `89fc6674ab3f37d9`; verifier pass; security pass (Critical 0, High 0) | done | `87b1e99` |
| US-0 | AC-2 | 实现私钥安全存储接口（接口层 + 真实 CNG 集成）| `src/coevo/identity/private_keys.py`; `src/coevo/identity/__init__.py`; `scripts/store_private_key.ps1` (Windows CNG non-exportable RSA-2048 pinned by F6DE) | `tests/security/test_private_key_storage.py` (21 项，含启动链投毒回归); `tests/integration/private_key_windows_store_test.py` (5 项真实 CNG end-to-end，含实际公钥摘要绑定; skip 若 F6DE 不可达); `tests/unit/test_private_key_handles_bindings.py` (5 项，固定运行时收据不跟踪/必须忽略及 metadata-only schema) | quality pass `34fc0b672c25a7b5`; 治理/私钥安全 26/26；真实 CNG 5/5；收据仅本地保留且 Git 不跟踪；security-reviewer PASS（Critical/High/Medium/Low 0/0/0/0）；audit fully-sealed | done | `d74f7b2` + 本次提交 |
| US-5 | AC-1 | `.agent` 36-byte Fixed Header + canonical Envelope；严格拒绝非规范 JSON、未知枚举/flags、长度矛盾、非法 nonce、尾随数据；SM2 仅保留后续 AC 的 fail-closed 注册表 | `src/coevo/protocol/agent_package.py`; `src/coevo/protocol/__init__.py`; `src/coevo/protocol/sm2_extension.py` | `tests/integration/package_header_test.py` (56 项); `tests/integration/package_header_extended_test.py` (3 项) | quality pass `e050cf72f6cda47e`; protocol 59/59；protocol-reviewer PASS（阻断/高/中/低 0/0/0/0）；security-reviewer PASS（Critical/High/Medium/Low 0/0/0/0）；audit fully-sealed | done | `d74f7b2` + 本次提交 |

工程环境切片不推进任何产品故事。RSA-3072/SHA-256 签名仅限开发原型; 正式环境必须替换为批准的 SM2 产品。

| US-1 | AC-1 | 任务流程理解 data-model + 多 schema 解析器 + 阶段映射层；strict monotonic int version + ISO-8601 UTC `Z` 后缀；每字段 source_path + confidence ∈ [0,1] + SourceKind ∈ {LITERAL, DERIVED, DEFAULTED, OVERRIDDEN}；reviewer 编辑经 Override 升 version；tabular/tree/canonical 三 schema；27 条默认 stage_hint → StandardStage 映射 | `src/coevo/task_flow/__init__.py`; `src/coevo/task_flow/models.py`; `src/coevo/task_flow/parser.py`; `src/coevo/task_flow/mapping.py` | `tests/unit/test_task_flow_models.py` (18 项：AC-1..AC-7 + 5 negative + 2 override + 3 mapping) | unit 56/56 ok (含 18 新增); compileall exit 0; audit_log verify ok=true; audit_seal fully-sealed; argv fp `34fc0b672c25a7b5` 双绿 | done | 本次提交 |
| US-1 | AC-2 | 任务流程理解 service-layer；FlowUnderstandingService 单一 facade（understand / confirm / to_audit_record）+ StageGraph（stage_ids_in_order / stage_membership / node_to_stage / standard_stage_by_node）+ ReviewerView（source_mapping_lookup / confidence_for）+ TaskFlowValidationError 显式 fail-closed；27 项 service 测试覆盖 4 个 schema 端到端 / graph / reviewer / confirm / audit_record / 7 项失败路径；不引入 IO / 不引入 LLM / 不改协议 / 不动审计链 | `src/coevo/task_flow/service.py`; `src/coevo/task_flow/__init__.py` (re-export: Override, FlowUnderstanding, FlowUnderstandingService, ReviewerView, StageGraph, TaskFlowValidationError) | `tests/unit/test_task_flow_service.py` (27 项：4 end-to-end + 5 graph + 4 reviewer + 3 confirm + 4 audit_record + 7 failure) | unit 85/85 ok (含 27 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed; audit fully-sealed | done | `0ca3478` |
| US-2 | AC-1 | 任务分解 data-model + 依赖图 + 基线版本；ProjectBaseline frozen dataclass（strict monotonic int version + ISO-8601 UTC `Z`）+ DependencyEdge（"fs" kind closed set）+ DependencyGraph（cycle fail-closed + 确定性 topo order + stage-order edge 自动 seed）+ build_baseline / confirm_baseline + TaskDecompositionService（消费 US-1 FlowUnderstanding，按 standard_stage 分组 + 锁定 process_flow_ref）+ to_audit_record；23 项 unit 覆盖 input validation / graph cycle / baseline version / service propose / audit_record | `src/coevo/task_decomposition/__init__.py`; `src/coevo/task_decomposition/models.py`; `src/coevo/task_decomposition/dependency_graph.py`; `src/coevo/task_decomposition/baseline.py`; `src/coevo/task_decomposition/service.py` | `tests/unit/test_task_decomposition.py` (23 项：5 input validation + 6 dependency graph + 6 baseline version + 6 service) | unit 110/110 ok (含 23 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `adbe286` |
| US-3 | AC-1 | 脱敏人才库最小集 + 候选推荐确定性层；TalentPool frozen dataclass（schema_version=1.0 + talent_code 唯一）+ Talent（field-minimum: talent_code / skill_tags / credentials / current_task_count / max_parallel_tasks / availability / RedactedIdentity）+ RedactedIdentity（pool_code + display_hint ≤16 + SHA-256 identity_hash）+ 不可逆脱敏（红 action + display_hint bounded）+ 评分算法（skill +2.0 / credential +1.0 / window full +1.5 / window partial +0.5 / load +1.0）+ LoadAlert（AT_CAPACITY / OVER_CAPACITY / WINDOW_CONFLICT）+ Recommendation + TalentRecommenderService facade + to_audit_record；32 项 unit 覆盖 model field / redaction / ranking / reasons / load alerts / service | `src/coevo/talent/__init__.py`; `src/coevo/talent/models.py`; `src/coevo/talent/redaction.py`; `src/coevo/talent/recommender.py`; `src/coevo/talent/service.py` | `tests/unit/test_talent_recommender.py` (32 项：11 model field + 6 redaction + 6 ranking + 3 reason + 3 load + 3 service) | unit 144/144 ok (含 32 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `b4382b6` |
| US-5 | AC-2 | `.agent` 协议 § 7.3 SM2 key wrap + § 7.4 SM4 AEAD payload + § 9/§ 12 manifest signature + § 17 replay detection 的 wire surface；P1 fail-closed（已留位 approved SM2/SM4 product 接入） | `src/coevo/protocol/agent_payload.py`; `src/coevo/protocol/sm2_sign.py`; `src/coevo/protocol/sm2_keywrap.py`; `src/coevo/protocol/replay_detector.py`; `src/coevo/protocol/package_builder.py`; `src/coevo/protocol/__init__.py` re-exports 28 new types | `tests/integration/test_agent_package_aead.py` (35 项：8 payload block + 7 key transport + 10 signature + 7 replay + 3 builder round-trip) | unit 144/144 ok; integration 107/107 ok (含 35 新增); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `0e8e728` |
| US-5 | AC-3 | `.agent` 协议 § 15 原子导入 7 步事务状态机 + § 17 持久化层 + 端到端 PackageImportService facade | `src/coevo/protocol/import_transaction.py`; `src/coevo/protocol/processed_package_store.py`; `src/coevo/protocol/import_service.py` | `tests/integration/test_agent_package_atomic_import.py` (23 项：11 import transaction + 5 processed package store + 7 import service facade) | unit 146/146 ok; integration 130/130 ok (含 23 新增); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `89c2d4e` |
| US-6 | AC-1 | 工作区初始化最小数据层：路径策略（QuarantinePath / WorkspacePath / WorkspacePaths，含 safe-id 验证 + no-traversal 拒绝）+ in-memory WorkspaceRegistry（refuse duplicate (project, role) + AC-8 idempotence rule）+ WorkspaceInitService facade（消费 US-5-AC-3 ImportOutcome → InitOutcome；支持 AC-2 隔离 / AC-4 非 committed 拒 / AC-5 项目角色目录 / AC-7 重复拒 / AC-8 同包幂等）+ to_audit_record；30 项 unit 覆盖路径策略 / 注册表 / init facade | `src/coevo/workspace/__init__.py`; `src/coevo/workspace/paths.py` (sanitize_id + QuarantinePath + WorkspacePath + WorkspacePaths + build_paths); `src/coevo/workspace/models.py` (WorkspaceRole + WorkspaceEntry + WorkspaceRegistry + InitOutcome + 3 exception classes); `src/coevo/workspace/init_service.py` (WorkspaceInitService facade) | `tests/unit/test_workspace_init.py` (30 项：5 QuarantinePath + 8 WorkspacePath + 5 WorkspaceRegistry + 9 init facade + 3 build_paths) | unit 178/178 ok (含 30 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `159f0db` |
| US-9 | AC-1 | 成果汇报包生成最小数据层：ReportManifest（AC-1..AC-4）+ ReportArtifact + ReportOverride + ReportStatus enum + ReportSubmissionSequence 内存单调 counter + ReportBuilder facade（消费 US-2 ProjectBaseline + manifest → 委托 US-5 build_unsigned_package 复用 wire layout 实现 AC-5 一致性）+ ReportPackage + to_audit_record；25 项 unit 覆盖 artifact / manifest / sequence / builder / filename / override / audit 7 个 TestCase | `src/coevo/report/__init__.py`; `src/coevo/report/models.py`; `src/coevo/report/builder.py` | `tests/unit/test_report_builder.py` (25 项) | unit 205/205 ok (含 25 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | `f8aeb9e` |
| US-10 | AC-1 | 状态合并最小数据层：MergeDecision 枚举（ACCEPT / REJECT / HOLD / MANUAL）+ FieldMerge（field_path + original/submitted value + decision + reason）+ MergeRecord 持久化记录 + MergeProposal（new_baseline + record + accepted）+ MergeEngine facade（消费 US-9 ReportManifest + US-2 ProjectBaseline → 校验 AC-1/AC-3 身份 + 基线版本 + AC-7 不依赖时间戳覆盖 + AC-8 严格单调 version + AC-9 持久化记录 + to_audit_record）；19 项 unit 覆盖 validation / conflict / auto-merge / no-timestamp-override / new-revision / merge-record / idempotence 7 个 TestCase | `src/coevo/merge/__init__.py` (MergeEngine + MergeDecision + FieldMerge + MergeRecord + MergeProposal + MergeError + MergeValidationError) | `tests/unit/test_merge_engine.py` (19 项：4 validation + 2 conflict + 5 auto-merge + 2 no-timestamp-override + 2 new-revision + 3 merge-record + 1 idempotence) | unit 226/226 ok (含 19 新增 + 2 traceability); compileall exit 0; `make_quality_gate` ×2 exit=0 fingerprint=`6ba24930200fc687` 双绿; audit_log verify ok=true; audit_seal fully-sealed | done | 本次提交 (待 commit 拆分) | 本次提交 90927fa / 9c5f92c (audit-managed) / 0bdd916 (product) / 待 C+D 完成 (US-10 P1 fix Round-2: 257/257 unit + make_quality_gate x2 绿 + audit fully-sealed + fingerprint 6ba24930200fc687 + 独立复核 deleg_ff7e82c1 10/10 reverse probes PASS)
| US-11 | AC-1 | 风险预警最小数据层: RiskKind + SourceKind + Risk + RiskReport + RiskAnalyzer facade (US-10 MergeRecord + US-2 ProjectBaseline 鈫?三类风险 + 会议建议 + to_audit_record). Round-2 fix 2026-07-28: US-11 依赖的 merge-receipt repository 行级门禁 (payload/signature/receipt_id/receipt_hash 类型+格式+长度) + 逐行 cursor 读取 (禁用 fetchall) + pre-INSERT 原子性 (行级 size cap 触发时不写新行，不 promote anchor). | `src/coevo/risk/__init__.py`; `src/coevo/merge/repository.py` (_ROW_PAYLOAD_MAX_BYTES = _RECEIPT_MAX_BYTES; _ROW_SIGNATURE_MAX_BYTES=1024; _ROW_SIGNATURE_MIN_BYTES=1; _ROW_RECEIPT_ID_PREFIX="mcr."; _ROW_RECEIPT_ID_HEX_LEN=64; _ROW_RECEIPT_ID_MAX_LEN=68; _ROW_HASH_HEX_LEN=64; _ROW_PROJECTION 受限列投影; _validate_row_shape 行级预检; _iter_verified_history 逐行 cursor 读取) | `tests/unit/test_risk_analyzer.py`; `tests/unit/test_merge_commit_receipt.py`; `tests/security/test_merge_receipt_repository.py`; `tests/integration/test_merge_risk_receipt_chain.py` | unit 279/279 ok; security 91/91 ok; integration 133/133 ok; e2e 3/3 ok; compileall exit 0; `make_quality_gate` x2 exit=0 fingerprint=`6ba24930200fc687` 稳定双绿; audit_log verify ok=true; audit_seal fully-sealed (sequence=346, signer_thumbprint F6DE13A4ADF56B9D66902B8E3055DCCA8B702D86, audit_byte_count 211663→216936); traceability US-11 checked=1 missing=0 | done | 本次提交 |
| US-12 | AC-1 | 监督/会议协调 service facade：消费 US-11 RiskReport → 督办项集 + 升级建议 + 会议候选议题 → 会议结论三类投影（任务/风险处置/督办项）；9 项 AC 全覆盖；不实际召集会议只产出建议；纯函数无 IO/LLM；to_audit_record 排除 basis/recommendation/rationale/closing_condition 敏感字段。 | `src/coevo/supervision/__init__.py` (SupervisionCoordinator + SupervisionItem + EscalationSuggestion + MeetingAgendaItem + MeetingProposal + MeetingConclusionProjection + SupervisionOutcome + COORDINATION_RECOMMENDED_KINDS + SUPERVISABLE_RISK_KINDS) | `tests/unit/test_supervision_meeting.py` (10 项: 3 model validation + 6 coordinator + 1 constants) | unit 289/289 ok (US-12 10 + 279 既有); compileall exit 0; `make_quality_gate` 脳脳 exit=0 fingerprint=`6ba24930200fc687` 稳定; audit_log verify ok=true; audit_seal fully-sealed (sequence=353, signer_thumbprint F6DE13A4ADF56B9D66902B8E3055DCCA8B702D86); traceability US-12 checked=1 missing=0 | done | 本次提交 |
| US-13 | AC-1 | 决策简报 service facade：只消费 latest verified merge receipt 与 owner-key signed RiskConfirmation；四区块结论逐项绑定 task/result/risk/receipt 来源；支持 stage/periodic/risk-topic；DecisionBriefRepository 以 revision+head digest CAS、event 幂等和内容哈希链保存完整修订历史；ApprovedTemplateRegistry 每次实际复验受控 DOCX，WPS 请求只生成新版本副本并强制人工确认；风险/模板/简报均有硬上限与脱敏审计投影。 | `src/coevo/decision_brief/__init__.py` | `tests/unit/test_decision_brief.py` (20 项：权威风险确认与 owner 四方身份绑定、baseline task membership、四区块/三类型、来源追踪、输入上限、CAS/旧事件重放/哈希链、模板篡改/宏/替代 registry、WPS 审核、审计脱敏；另 1 symlink 反例按当前 Windows 权限条件跳过) | 目标及直接依赖 54 项执行：53 pass / 1 conditional skip / 0 fail；完整锁定门禁 exit=0 fingerprint=`34fc0b672c25a7b5`；security 91/91；e2e 3/3；audit seal fully-sealed；独立 mvp-verifier PASS；独立 security-reviewer Critical/High/Medium/Low=0/0/0/0 PASS | done | 本次提交 |
| US-8 | AC-1 | 进展采集最小数据层：EvidenceInput/EvidenceRef (AC-7 排除 FILE_MTIME_ONLY，4 种 evidence kind 闭集) + ProgressItem (AC-3 强制 ≥1 evidence_ref；AC-4 source_kind+confidence ∈ [0,1]) + ProgressCapture (AC-6 requires_user_confirmation=True 强制，formally_accepted=False 默认) + ProgressCaptureService (extract_progress / revise / reject / accept / to_report_draft / to_audit_record) + ProgressDraft (AC-8 仅 formally_accepted 才生成，按 ProgressItemKind 分桶 4 段，绑定 item_id) + 纯函数无 IO；to_audit_record 排除 text/confidence/overrides.reason。 | `src/coevo/progress_capture/__init__.py` (898 行) | `tests/unit/test_progress_capture.py` (29 项：model constants + EvidenceKind closed-set + extract_progress 12 项 + gate 6 项 + revise/reject 6 项 + report_draft 2 项 + audit projection 1 项) | unit 29/29 ok; compileall exit 0; `scripts/dev.ps1 -Task quality` exit=0 fingerprint=`34fc0b672c25a7b5` 稳定; audit_log verify ok=true; audit_seal fully-sealed (sequence=373, signer_thumbprint F6DE13A4ADF56B9D66902B8E3055DCCA8B702D86, audit_byte_count 236835); traceability US-8 checked=N missing=0 | done | 本次提交 b3de03a + 后续 state-sync + audit-finalize | 本次提交 b3de03a (code+test) / 待 state-sync + audit-finalize 落 commit |
| US-15 | AC-1 | 安全审计 facade（异常包拦截决策 + 日志字段统一 + 安全管理员查询导出）：AuditEvent 强制 ts/actor/action/result/project_id/task_id 字段（AC-5 fail-closed）；InterceptionReason 5 种闭集（CORRUPTED/TAMPERED/EXPIRED/DUPLICATE/RECIPIENT_MISMATCH） + SecurityAuditFacade.evaluate_interception 集中决策（AC-1）；AuditQuery 6 字段过滤 + limit (hard cap 10000) + cursor (record_hash) 翻页 + export_events JSON/JSONL + SHA-256 content-stable digest（AC-6）；to_audit_record 排除 detail 文本只保留 detail_hash + reasons 列表；纯函数无 IO。 | `src/coevo/audit_governance/__init__.py` (702 行) | `tests/unit/test_audit_governance.py` (29 项：AuditEvent 6 项 + Interception 10 项 + Query 8 项 + Export 4 项 + AuditProjection 1 项) | unit 29/29 ok; compileall exit 0; `python scripts/quality_gate.py --target quality` exit=0 fingerprint=`6ba24930200fc687` 稳定; audit chain fully-sealed sequence=378 (line_count=535, byte_count=237481, signed_at=2026-08-01T00:16:42Z, signer_thumbprint F6DE13A4ADF56B9D66902B8E3055DCCA8B702D86, tail match) | done | 本次提交 90cc252 + 后续 state-sync + audit-finalize |
| US-4 | AC-1 | 运行中枢最小编排 facade：AgentSpec 不可变登记 (agent_id/capability/display_name/input_schema/output_schema/requires_human_confirmation) + AgentCapability 11 类闭集 (US-1/2/3/5/8/9/10/11/12/13/15) + AgentRegistry 不可变 (register/get/list_available/by_capability/set_status) + AgentStatus 闭集 (AC-1/AC-2) + OrchestrationStep (AGENT_CALL/HUMAN_CONFIRM/CONDITIONAL) + FailurePolicy (RETRY/SKIP/ESCALATE_HUMAN) + OrchestrationEvent + OrchestrationTrace + OrchestrationReport + Orchestrator.dispatch_event (AC-3/AC-4/AC-5/AC-6) + Orchestrator.confirm_human (AC-5) + MVP_FIXED_CHAIN 5 步固定编排链 + to_audit_record 排除 detail 文本只保留 detail_hash (AC-7); 纯函数无 IO。 | `src/coevo/orchestrator/__init__.py` (861 行) | `tests/unit/test_orchestrator.py` (26 项：AgentSpec/Registry 7 项 + Dispatch 9 项 + ConfirmHuman 4 项 + AuditProjection 1 项 + MVPFixedChain 2 项 + PureFunction 1 项 + 跨类组合 2 项) | unit 26/26 ok; compileall exit 0; `python scripts/quality_gate.py --target quality` exit=0 fingerprint=`6ba24930200fc687` 稳定; audit_log verify ok=true; audit_seal fully-sealed sequence=381 (line_count=537, byte_count=238127, signed_at=2026-08-01T00:47:42Z, signer_thumbprint F6DE13A4ADF56B9D66902B8E3055DCCA8B702D86, tail match) | done | 本次提交 bae1d58 + 后续 state-sync + audit-finalize |
| US-4 | AC-2 | 真实调用 US-1/2/3 facade，冻结任务包预览并经有权人工确认；SQLite 保存幂等状态、恢复态与哈希链审计。业务负责人 2026-08-02 批准 GmSSL 3.2.0 作为 MVP 原型 provider 范围（`mvp-prototype`，非生产密码产品）：`resume_real_chain` 注入原型 provider 后真实完成 SM3 摘要、SM2 签名/验签、SM2 会话密钥封装与 SM4-GCM 密封，生成可解析、可解封、签名验真的 `.agent` 字节并仅在回环校验通过后标记 COMPLETED；无 provider 时仍返回 `CRYPTO_CAPABILITY_UNAVAILABLE / ESCALATED`，绝不伪完成。 | `src/coevo/orchestrator/_real_chain.py`; `src/coevo/orchestrator/real_chain_store.py`; `src/coevo/orchestrator/__init__.py`; `src/coevo/protocol/package_builder.py`; `src/coevo/protocol/sm2_sign.py`; `src/coevo/crypto/gmssl_provider.py`; `scripts/generate-sm2-test-pki.ps1`; `scripts/gmssl-test-pki-helper.cs`; `scripts/gmssl-crypto-helper.cs`; `scripts/invoke-gmssl-crypto.ps1` | `tests/integration/test_orchestrator_real_facade_chain.py` (10 项); `tests/unit/test_real_chain_store.py` (18 项); `tests/integration/test_gmssl_prototype_provider.py` (5 项，含真实包回环与 orchestrator COMPLETED 路径); `tests/integration/test_sm2_test_pki_generation.py` (25 pass / 1 条件 skip) | 定向 18+10+5+25=58/58 (1 skip)；`scripts/dev.ps1 -Task quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；security-reviewer PASS 0/0/0/0（2026-08-01 独立复核 + 2026-08-02 内联复验） | done | 本次提交（业务负责人已批准 MVP 原型 provider 范围） |
| US-7 | AC-1 | 本地驾驶舱 facade（环回绑定 + 静态本地化 + 项目/角色/任务/里程碑视图 + WPS 允许列表 + 状态保持快照）：CockpitServerConfig 强制 bind_host='127.0.0.1'（AC-1 fail-closed，构造时拒绝 0.0.0.0/10.0.0.1）+ static_root 必须 src/coevo/cockpit/static/ 内（AC-2）+ CockpitFacade.dispatch 7 类路由（LIST_PROJECTS/LIST_ROLES/PROJECT_VIEW/ROLE_VIEW/TASK_VIEW/MILESTONE_VIEW/WPS_OPEN）+ WPSAllowList 扩展名 + MIME 双层白名单（.docx/.doc/.xlsx/.xls/.pptx/.ppt/.rtf/.pdf 通过；.exe/.bat/.ps1/.js/.vbs/.scr/.jar 拒绝）+ path traversal 拒绝（AC-4）+ CockpitServerState 不可变快照（AC-9）+ to_audit_record 排除 body_html/artifact_path 只保留 16-hex hash；纯函数无 IO。 | `src/coevo/cockpit/__init__.py` (801 行) + `src/coevo/cockpit/static/index.html` (placeholder) | `tests/unit/test_cockpit.py` (22 项：环回绑定 2 + 静态目录 3 + LIST_PROJECTS 1 + 角色视图 4 + 任务里程碑 3 + WPS 5 + NOT_BOUND 2 + 审计投影 1 + 纯函数 1) | unit 22/22 ok; compileall exit 0; `python scripts/quality_gate.py --target quality` exit=0 fingerprint=`6ba24930200fc687` 稳定; audit chain sequence=381+ (line_count=540+, byte_count 持续递增, signer_thumbprint F6DE) | done | 本次提交 a487105 + 后续 state-sync + audit-finalize |
| US-14 | AC-1 | 成果沉淀 facade（汇总 + 复盘草稿 + 模板提取 + 来源标记 + 密级检查 + 用户审核 + fail-closed 入库）：KnowledgeBaseFacade.aggregate 消费 baseline + merge_records + risk_reports + meeting_conclusions + decision_briefs + progress_captures + model_summaries -> KnowledgeEntry 列表；RetrospectiveDraft 5 段（总体进展/重要变化/高风险/待决策/最佳实践）+ requires_user_review=True 强制；ReusableTemplate 3 类（PROCESS_TEMPLATE/TASK_TEMPLATE/RISK_RULE）从 baseline + risk_reports 自动抽；KnowledgeClassification 4 级 + check_classification（actor_clearances fail-closed）；ReviewDecision APPROVE/REVISE/REJECT；review() 仅当所有 MODEL_SUMMARY 已决策且非拒绝时才 formally_committed=True；KnowledgeBundle.requires_user_confirmation=True + formally_committed=False 默认，构造时强制；to_audit_record 排除 body_summary / body_sections / template body，只保留 SHA-256 16-hex hash。 | `src/coevo/knowledge_base/__init__.py` (892 行) | `tests/unit/test_knowledge_base.py` (24 项：闭集 2 + aggregate 4 + retrospective 3 + template 2 + review 6 + classification 2 + construct 2 + audit projection 1 + pure-function 1 + 跨类组合 1) | unit 24/24 ok; compileall exit 0; `python scripts/quality_gate.py --target quality` exit=0 fingerprint=`6ba24930200fc687` 稳定; audit chain fully-sealed (sequence=383+, signer_thumbprint F6DE, tail match) | done | 本次提交 d878836 + 后续 state-sync + audit-finalize |
| US-9 | AC-3-fix | ReportBuilder 发送端 base_revision 校验 fail-open 修复：manifest.base_revision 必须等于基线主版本 <project_id>-R<version:04d>（协议 §16.1，与 merge._master_revision 格式一致），不匹配即抛 ReportManifestValidationError（AC-3 fail-closed，原实现为 pass 静默接受） | `src/coevo/report/builder.py` | `tests/unit/test_report_builder.py`（28 项：原 25 项 + 新增 mismatch 拒绝 / 规范版本接受 / merge 格式锁定） | quality pass `34fc0b672c25a7b5`; audit fully-sealed; security-reviewer PASS 0/0/0/0 | done | 本次提交 |
| US-4 | AC-2-PATH | 获批密码产品/受保护密钥句柄接入路径：`CryptoProvider` 结构性契约（sm3/sign/verify/seal/open + `ProviderScope` 作用域治理，原型=`mvp-prototype`、正式产品=`approved-product`，`validate_provider_scope` fail-closed）；离线导入/哈希锁定/安全与协议复核/门禁收口流程见 `docs/dependencies/approved-crypto-provider-path.md`；正式产品必须经 `APPROVED_PRODUCT` 作用域且私钥不可导出（CNG/SKF/PKCS#11/HSM），禁止运行时下载与静默回退原型。 | `src/coevo/crypto/contract.py`; `src/coevo/crypto/__init__.py`; `src/coevo/crypto/gmssl_provider.py`; `docs/dependencies/approved-crypto-provider-path.md` | `tests/unit/test_crypto_contract.py` (6 项) | unit 6/6 ok；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | 本次提交 |
| US-7 | AC-2 | 本地驾驶舱真实 HTTP 服务：`http.server` 线程服务器强制环回绑定（AC-1）；本地身份令牌 + 会话管理（仅存 SHA-256 摘要、不活动超时、有界会话表）；每请求 Host 校验、状态变更请求 Origin + `X-Requested-With` 双通道 CSRF 防护；写操作（wps_open）要求显式 confirm=true 二次确认；静态资源仅从 cockpit/static 提供（扩展名白名单 + 穿越拒绝 + 大小上限 + CSP/nosniff/no-store）；真实 HTML/JS 渲染项目/角色/任务/里程碑/工件（app.js 调本地 JSON API，令牌存 sessionStorage）；每请求经 to_audit_record 进入有界内存审计；可选单实例锁文件。WPS 子进程留给 US-7-AC-4。 | `src/coevo/cockpit/server.py`; `src/coevo/cockpit/__init__.py`; `src/coevo/cockpit/static/index.html`; `src/coevo/cockpit/static/app.js`; `src/coevo/cockpit/static/style.css` | `tests/integration/test_cockpit_http_server.py` (15 项); `tests/unit/test_cockpit_http.py` (22 项) | unit 22/22 + integration 15/15；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；security-reviewer PASS 0/0/0/0 | done | 本次提交 |
| US-7 | AC-3 | 驾驶舱状态持久化：`CockpitStateStore` JSON 原子写（临时文件 + fsync + os.replace，失败不留 .tmp），加载 fail-closed（缺失返回 None；损坏/超大/未知字段/重复键/错误 schema 一律抛错，绝不静默清空）；`CockpitHttpConfig.state_path` 接入 HTTP 服务器生命周期——启动无显式视图时从磁盘加载，停止时落盘；显式视图优先于持久化状态。 | `src/coevo/cockpit/state_store.py`; `src/coevo/cockpit/server.py`; `src/coevo/cockpit/__init__.py` | `tests/unit/test_cockpit_state_store.py` (13 项); `tests/integration/test_cockpit_state_persistence.py` (3 项) | unit 13/13 + integration 3/3 + AC-2 回归 15/15；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | 本次提交 |
| US-8 | AC-2 | 实时文件 watcher：轮询快照差异检测 CREATED/MODIFIED/DELETED；写入稳定判定（size+mtime+digest 连续 stability_checks 次一致才发事件）；有界事件队列 + 可选后台线程；路径安全（相对路径、符号链接跳过、解析后必须在根内）；隐藏文件跳过、扩展名过滤、超大文件免摘要（不可作证据，fail-closed）；绝不产生 mtime-only 完成信号（AC-7）；`build_evidence_input` 将事件映射为 DOCUMENT_CONTENT/ARTIFACT_FILE 证据并接入 ProgressCaptureService.extract_progress。 | `src/coevo/progress_capture/watcher.py`; `src/coevo/progress_capture/__init__.py` | `tests/unit/test_progress_watcher.py` (19 项); `tests/integration/test_progress_watcher.py` (2 项) | unit 19/19 + integration 2/2；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | 本次提交 |
| US-14 | AC-2 | 知识库持久化入库：`KnowledgeStore` SQLite 存储（显式 create/open，禁止从请求数据隐式建库）；schema 哈希锁定（meta.schema_sha256 + DDL 规范化比对 + 拒绝额外表/视图/触发器/索引/列漂移，隐式 sqlite_autoindex 跳过）；bundle 规范 JSON 序列化（严格注册表、未知类型/字段/枚举拒绝、$tuple/$list 标记、重建时触发 __post_init__ 校验）；save 幂等（同 id+digest）与冲突拒绝（同 id 异 digest）；load 校验摘要与结构、缺失返回 None、损坏抛错（fail-closed）；追加式哈希链审计（仅 id/摘要，无自由文本内容）；8MB 负载上限。 | `src/coevo/knowledge_base/store.py`; `src/coevo/knowledge_base/__init__.py` | `tests/unit/test_knowledge_store.py` (17 项); `tests/integration/test_knowledge_store.py` (4 项) | unit 17/17 + integration 4/4；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；security-reviewer PASS 0/0/0/0 | done | 本次提交 |
| US-15 | AC-2 | 安全管理员实时审计流：`AuditStreamHub` 进程内发布/订阅中枢（push 回调 + 有界缓冲队列）；订阅校验（actor safe-id、callback 可调用、filter 可调用或 None、max_queued>0、订阅数上限）；发布仅接受 AuditEvent（fail-closed）；投递故障隔离（单个回调异常不影响其它订阅者，计入 callback_errors）；缓冲溢出 drop-oldest 且 dropped 计数（丢弃不静默）；有界 recent 历史；线程安全（RLock + 快照投递），并发发布不丢事件。持久化仍由既有审计链存储负责，本切片为实时通道。 | `src/coevo/audit_governance/stream.py`; `src/coevo/audit_governance/__init__.py` | `tests/unit/test_audit_stream.py` (10 项); `tests/integration/test_audit_stream.py` (5 项) | unit 10/10 + integration 5/5；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；security-reviewer PASS 0/0/0/0 | done | 本次提交 |
| DEMO | AC-1 | 离线演示 runner：`src/coevo/app` composition root 把 US-1/2/3 真实五步链 + 人工确认 + US-5 打包串成可运行闭环——生成真实加密 `.agent` 包并解析/解封/验签回环、导出 outbox；可选启动本地 cockpit HTTP 服务；知识库聚合后持久化入库；审计事件发布到 AuditStreamHub。`scripts/run_demo.py` CLI 提供 `--smoke` 无头自检。演示专用件（DemoSigner/DemoFreshnessAuthority）显式标注非生产；GmSSL 原型仅以 mvp-prototype 作用域使用。 | `src/coevo/app/__init__.py`; `scripts/run_demo.py` | `tests/e2e/test_demo_runner.py` (3 项) | e2e 3/3；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | — |
| US-4 | AC-2-PATH2 | 获批密码接入路径：`ProviderRegistry` 命名注册 + `require_approved` fail-closed（scope=APPROVED_PRODUCT 且 key_handle_backed=True）；`ProtectedKeyHandle`/`KeyHandleBacked` 契约要求不可导出受保护密钥句柄（CNG/SKF/PKCS#11/HSM）；GmSSL 原型显式 key_handle_backed=False，无法冒充获批产品。 | `src/coevo/crypto/contract.py`; `src/coevo/crypto/key_handle.py`; `src/coevo/crypto/__init__.py`; `src/coevo/crypto/gmssl_provider.py` | `tests/unit/test_crypto_provider_registry.py` (9 项) + `tests/unit/test_crypto_contract.py` (6 项) | unit 15/15；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | — |
| ENG-BASE | HISTORY-SCRUB-1 | 授权清理 git 历史私钥句柄收据：`git filter-branch --index-filter 'git rm --cached --ignore-unmatch ...' --prune-empty` 移除 `loop/private-key-handles-*.json`（4 个历史 blob、跨全部本地 refs），随后清理 refs/original、reflog、gc；`git rev-list --all --objects` 无匹配路径、`git fsck --full` 干净、旧 HEAD 85d07b7 不可解析；`.gitignore` 永久排除该模式。 | `loop/DECISIONS.md`; `.gitignore` | `tests/security/test_private_key_handles_bindings.py` (4 项) | 4/4；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | — |
| ENG-BASE | BENCH-AC-1 | 离线性能基准套件：`src/coevo/benchmarks` 纯框架（SLA_TARGETS 表 + measure/report）+ `scripts/benchmark.py` CLI（--check 对照 SLA 失败即非零）。2026-08-02 实测：page_open 0.000s（≤3s）、task_query 0.000s（≤2s）、dir_discovery 0.276s（≤5s）、package_check 0.0001s（≤10s）、package_generation 100%（≥95%），all_ok=true。基准不进质量门禁（计时环境相关），由人工/CI 按需执行。 | `src/coevo/benchmarks/__init__.py`; `scripts/benchmark.py` | `tests/unit/test_benchmark_suite.py` (8 项) | unit 8/8；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | 本次提交 |
| US-7 | OPS-1 | 驾驶舱运维：/healthz 无鉴权探活端点（环回+Host 校验后返回 status/uptime，无敏感数据）；CockpitHttpConfig.log_path 开启 JSONL 访问日志落盘（每请求 audit 记录追加写、写失败仅计数不阻断）；单实例锁默认开启（LOCALAPPDATA/KaiwuAgent/cockpit.lock，含 mtime 超时陈旧锁接管）。 | `src/coevo/cockpit/server.py` | `tests/integration/test_cockpit_http_server.py`（含 healthz/日志落盘）; `tests/unit/test_cockpit_http.py`（含默认锁/陈旧锁） | unit 24/24 + integration 18/18；`make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed | done | 本次提交 |
| ENG-BASE | REFACTOR-AUDIT-1 | 巨型单文件拆分：src/coevo/audit_governance 由 717 行单体拆为 models.py（454 行）+ facade.py（250 行）+ __init__.py 再导出（36 行），公共 API 与行为不变（编译+29+10 测试通过）。 | `src/coevo/audit_governance/models.py`; `src/coevo/audit_governance/facade.py`; `src/coevo/audit_governance/__init__.py` | `tests/unit/test_audit_governance.py`; `tests/unit/test_audit_stream.py` | unit 29/29 + 10/10；compileall exit 0；`make quality` 全绿 | done | 本次提交 |
| US-7 | AC-4 | WPS 受控子进程启动层：路径须为工作区相对且通过 WPSAllowList；解析后必须位于 workspace_root 内且为普通文件（符号链接/reparse 拒绝）；可执行文件显式（COEVO_WPS_EXE 或注入 runner），缺失返回 NOT_AVAILABLE；dry-run 全检查不启动；不接受包控制任意启动参数。 | `src/coevo/cockpit/wps.py`; `src/coevo/cockpit/__init__.py` | `tests/unit/test_wps_launcher.py` (9 项) | unit 8/8 + 1 条件 skip；`make quality` 全绿 | done | 本次提交 |
| US-7 | FRONTEND-E2E-1 | 离线前端 E2E：真实浏览器工具本会话不可用（已留痕），回退为 HTTP+静态策略验证——索引页本地资源链接与 CSP、静态资源 200 且 JS 无外部 URL、API 端点驱动 UI、未知资源 404。 | `tests/e2e/test_cockpit_offline_frontend.py`; `src/coevo/cockpit/static/app.js` | `tests/e2e/test_cockpit_offline_frontend.py` (4 项) | e2e 4/4；`make quality` 全绿 | done | 本次提交 |
| ENG-BASE | WIN7-AC-1 | Win7 兼容独立分支专项：独立分支 win7-compat；docs/architecture/win7-compat-branch.md 定义冻结依赖/功能范围（支持/降级/不支持）/安全补偿/测试计划；tests/win7 专项（stdlib-only 导入、禁 runtime 依赖、离线约束）4/4；scripts/win7-compat-check.ps1。 | `docs/architecture/win7-compat-branch.md`; `tests/win7/test_win7_compat_profile.py`; `scripts/win7-compat-check.ps1` | `tests/win7/test_win7_compat_profile.py` (4 项) | win7 专项 4/4；`make quality` 全绿 | done | 本次提交（实机验证状态 pending-win7-runtime） |
| US-15 | STREAM-PERSIST-1 | 审计流持久化与订阅鉴权：AuditStreamStore（JSONL + SHA-256 哈希链、显式 create/open、篡改拒开、大小上限、事件回放）；AuditStreamHub 接入 store（发布先持久化，失败 fail-closed）与 authorizer（订阅需 is_allowed(actor, permission=audit:subscribe)）；replay=True 对新订阅者回放历史。 | `src/coevo/audit_governance/stream_store.py`; `src/coevo/audit_governance/stream.py`; `src/coevo/audit_governance/__init__.py` | `tests/unit/test_audit_stream_store.py` (5 项); `tests/integration/test_audit_stream.py` (7 项) | unit 5/5 + 既有 10/10 + integration 7/7；`make quality` 全绿 | done | 本次提交 |
| US-7 | SESSION-ROTATE-1 | 会话令牌轮换：CockpitSessionManager.rotate（撤销旧令牌并签发新令牌）+ max_session_age_sec（超期会话 validate 失败，强制重新登录/轮换）。 | `src/coevo/cockpit/server.py` | `tests/unit/test_cockpit_http.py` (27 项) | unit 27/27；`make quality` 全绿 | done | 本次提交 |
| ENG-BASE | RECORDS-ARCHIVE-1 | 记录归档策略：src/coevo/records_archive.py 纯函数（VERIFICATION/DECISIONS/tool-audit 分区 + archive_plan）；scripts/archive_records.py（--dry-run/--apply，按阈值归档到 loop/archive/YYYYMMDD/）；docs/process/records-archiving-policy.md；不动 audit-head 签名链。 | `src/coevo/records_archive.py`; `scripts/archive_records.py`; `docs/process/records-archiving-policy.md` | `tests/unit/test_records_archive.py` (5 项) | unit 5/5；`make quality` 全绿 | done | 本次提交 |
| ENG-BASE | REVIEW-SANDBOX-1 | 只读审查沙箱治理：scripts/review_sandbox.py（prepare=隔离 clone+pin 清单；check=HEAD 钉扎+除 loop/ 外全部受保护路径字节比对，违规即作废；discard=安全路径删除；沙箱位于 loop/runtime/review-sandboxes 且 gitignored）；docs/process/independent-review-governance.md（角色/放行标准/超时处置/越权作废）；切片计划与 10 项单元测试。 | `scripts/review_sandbox.py`; `docs/process/independent-review-governance.md`; `docs/plans/review-sandbox-slice.md` | `tests/unit/test_review_sandbox.py` (10 项) | unit 10/10；`make quality` 全绿；audit fully-sealed | done | 本次提交（真实独立复核执行留待具备只读沙箱/合规复核者的环境） |
| ENG-BASE | OPT-PERF-1 | 全盘性能审查与优化（2026-08-02）：① 依赖图拓扑排序修复 O(V·E)→O(E)（列表成员检查改集合）+ heap Kahn O((V+E)log V) + 迭代环检测（5000 节点无递归上限）+ DependencyGraph O(1) 邻接索引；② StageGraph/SourceMapping/ReviewerView/mapping hint→best-rule O(1) 索引 + 服务构造期规则预排序；③ TalentPool.by_code O(1) + 人才评分集合预热（O(R·N·S)→O(R·N)）；④ watcher 未变化文件免重哈希（reuse_digest_on_unchanged 默认开，严格模式可关）；⑤ ProcessedPackageStore/AgentRegistry 惰性 O(1) 查找缓存；⑥ cockpit 静态资源 mtime 校验有界缓存；⑦ agent_package.py 14 处与 package_header_test.py 1 处注释编码损坏修复；⑧ 5 项可扩展性基准探针（dag_toposort 7.18s→0.52s 同负载 13.7x；graph_lookup 0.001s；talent_recommend 0.04s；registry_lookup 0.07s；watcher_rescan 0.02s）。 | `src/coevo/task_decomposition/dependency_graph.py`; `src/coevo/task_flow/models.py`; `src/coevo/task_flow/mapping.py`; `src/coevo/task_flow/service.py`; `src/coevo/talent/models.py`; `src/coevo/talent/recommender.py`; `src/coevo/progress_capture/watcher.py`; `src/coevo/protocol/processed_package_store.py`; `src/coevo/orchestrator/__init__.py`; `src/coevo/cockpit/server.py`; `src/coevo/benchmarks/__init__.py`; `scripts/benchmark.py` | `tests/unit/test_optimizations.py` (18 项); `tests/unit/test_benchmark_suite.py` (11 项) | unit 624/624 (skipped=2) + integration 207/207 (skipped=1) + security/e2e 由 make quality 全量复核；`python scripts/benchmark.py --check` all_ok=true；audit fully-sealed | done | 本次提交（工作树内，未 commit） |
| ENG-LOOP-ENV | BIN-1 | 修复 `.tools\bin-<PID>` 生命周期（2026-08-02）：入口脚本每次会话在 `.tools\bin-<PID>` 编译 make shim 并加锁，但会话结束只释放句柄从不删除目录，长期累积 47+ 个残留。修复：① dev.ps1 / run-loop.ps1 以 try/finally 在任务结束（含失败与 exit 路径）显式调用 `Clear-CoevoDevelopmentEnvironment`（先关句柄，再按 目录名 `^bin-\d+$` + 位于 .tools 内 + 非重解析点 三重门禁删除）；② 入口启动时 `Remove-StaleDevelopmentEnvironmentBins` 清扫历史残留——仅删除目录名匹配、路径受控、非重解析点且对应 PID 已不存在的目录，正在运行会话的锁定目录安全跳过；③ 初始化失败路径 finally 兜底清理；④ 直接点加载（-Command/交互）无 finally 的场景由下一次入口清扫兜底（已实测验证 PowerShell.Exiting 事件动作在 -Command 模式不执行，故不采用该机制）。修复后全量测试运行零残留。 | `scripts/enter-dev-environment.ps1`; `scripts/dev.ps1`; `scripts/run-loop.ps1`; `docs/development-environment.md` | `tests/integration/test_dev_environment_entry.py` (4 项：新增 bin 退出后清除 + 残留清扫); `tests/security/test_local_toolchain_security.py` (17 项：新增静态门禁断言 + 8 处命令补显式清理) | integration 4/4 + security 17/17；`make quality` exit=0 fingerprint=`6ba24930200fc687`；audit fully-sealed；实测 before=47 leftover → 修复后 0 残留 | done | 本次提交（工作树内，未 commit） |
| ENG-BASE | PROD-HARDEN-1 | MVP 生产可用化（2026-08-03）：① 超长单文件拆分（按 REFACTOR-AUDIT-1 既有模式 models + service/facade/repositories + __init__ 再导出，公共 API/`__all__`/相对导入顺序不变）：decision_brief 1414→models/repositories/service、merge 1188→models/engine、orchestrator 988→models/service、progress_capture 915→models/service、knowledge_base 907→models/facade、cockpit 835→models/facade、cockpit/server 925→sessions/static/server；修复 merge `__all__` 既有瑕疵（补 canonical_baseline_digest 导入、移除不可导入的方法名）；② 生产支持层：`src/coevo/version.py`（语义版本 0.1.0，禁时间戳）、`src/coevo/config.py`（AppConfig.from_env 环境驱动、环回/端口/日志级别/路径失败关闭）、`src/coevo/logging_setup.py`（轮转文件+控制台、与审计链隔离）、`scripts/run_cockpit.py`（--check/--version、SIGINT/SIGTERM/SIGBREAK 优雅停机、状态落盘、退出码 2=配置错误）；③ run_demo 增 --version；④ docs/production-readiness.md；⑤ 拆分工具 .omo/split_packages.py/.omo/split_server.py（gitignored，AST 切片保留注释+自动裁剪导入+自动再导出）。 | `src/coevo/decision_brief/__init__.py`; `src/coevo/decision_brief/models.py`; `src/coevo/decision_brief/repositories.py`; `src/coevo/decision_brief/service.py`; `src/coevo/merge/__init__.py`; `src/coevo/merge/models.py`; `src/coevo/merge/engine.py`; `src/coevo/orchestrator/__init__.py`; `src/coevo/orchestrator/models.py`; `src/coevo/orchestrator/service.py`; `src/coevo/progress_capture/__init__.py`; `src/coevo/progress_capture/models.py`; `src/coevo/progress_capture/service.py`; `src/coevo/knowledge_base/__init__.py`; `src/coevo/knowledge_base/models.py`; `src/coevo/knowledge_base/facade.py`; `src/coevo/cockpit/__init__.py`; `src/coevo/cockpit/models.py`; `src/coevo/cockpit/facade.py`; `src/coevo/cockpit/sessions.py`; `src/coevo/cockpit/static.py`; `src/coevo/cockpit/server.py`; `src/coevo/version.py`; `src/coevo/config.py`; `src/coevo/logging_setup.py`; `scripts/run_cockpit.py`; `scripts/run_demo.py`; `docs/production-readiness.md` | `tests/unit/test_production_support.py` (13 项：版本/配置/日志/CLI)；既有 unit 637 + integration 209 + e2e 10 全绿（含拆分后回归） | unit 637/637 (skipped=2) + integration 209/209 (skipped=1) + e2e 10/10；`python scripts/benchmark.py --check` all_ok=true（拆分后无性能回退：dag_toposort 0.46s）；实测 run_cockpit 启动→/healthz 200→CTRL_BREAK 优雅退出码 0→状态/访问日志落盘；`make quality` exit=0；audit fully-sealed | done | 本次提交（工作树内，未 commit） |
| ENG-BASE | DELIVERY-1 | 交付收口（2026-08-03）：① 新增成果回传链 E2E `tests/e2e/test_return_chain.py`——真实 SM2/SM4 加密 RESULT_SUBMISSION 包（GmSSL 原型 provider）→ parse/open 解封验签 → 重放门禁 + 原子导入 COMMITTED → MergeEngine.merge_and_analyze（签名收据 + 新主版本 PRJ001-R0002）→ RiskConfirmationRepository 确认 → DecisionBriefService 生成阶段简报 → KnowledgeBaseFacade 聚合 + KnowledgeStore 持久化重开；补齐 GOAL.md 完成定义第 3 条（两条固定编排链 E2E：下发链 test_demo_runner + 回传链 test_return_chain）；② app 组合根拆分：`src/coevo/app/__init__.py` 564→demo_support（DemoSigner/DemoFreshnessAuthority/PKI 引导/样例输入，显式非生产）+ pipeline（run_demo_pipeline/DemoResult）+ __init__ 再导出，公共 API 不变；③ README.md 重写（现状/快速开始/文档/交付边界）；④ 最终安全自查：新模块无 eval/exec/shell/网络模式，唯一 subprocess 为既有固定脚本路径 demo PKI 引导；Critical/High=0/0。 | `tests/e2e/test_return_chain.py`; `src/coevo/app/__init__.py`; `src/coevo/app/demo_support.py`; `src/coevo/app/pipeline.py`; `README.md` | `tests/e2e/test_return_chain.py` (1 项全链断言); `tests/e2e/test_demo_runner.py` (3 项回归); `tests/unit/test_production_support.py` (13 项回归) | e2e 4/4（demo 3 + return 1）；unit 637 + integration 209 回归全绿；`make quality` exit=0 fingerprint=`6ba24930200fc687`；audit fully-sealed；安全自查 Critical/High=0/0 | done | 本次提交（工作树内，未 commit） |
| ENG-BASE | STABILITY-1 | 稳定性与运维加固（2026-08-03）：① GmsslPrototypeProvider._invoke 有界重试（默认 1 次额外尝试，0.25s×attempt 退避）——仅重试启动级瞬时失败（subprocess OSError/TimeoutExpired、helper 无 GCP-E-* 可识别诊断的 GCP-E-LAUNCH），helper 报告的具体密码级错误（如 GCP-E-SIGN）绝不重试；敏感请求 bytearray 以 try/finally 归零；② 生产启动器 E2E `tests/e2e/test_cockpit_launcher.py`：真实子进程（CREATE_NEW_PROCESS_GROUP）启动 run_cockpit → /healthz 200 → CTRL+BREAK → 退出码 0 → cockpit-state.json 与 cockpit-access.jsonl 落盘 → 单实例锁释放；AppConfig 新增 COEVO_LOCK_PATH（默认锁路径不受影响，仅显式配置才覆盖）；③ supervision 632 行拆分 models(18)/service(8) + __init__ 再导出（含 __all__），公共 API 不变；④ 版本 0.1.0→0.2.0（生产可用化里程碑）。 | `src/coevo/crypto/gmssl_provider.py`; `src/coevo/config.py`; `scripts/run_cockpit.py`; `tests/e2e/test_cockpit_launcher.py`; `src/coevo/supervision/__init__.py`; `src/coevo/supervision/models.py`; `src/coevo/supervision/service.py`; `src/coevo/version.py`; `docs/production-readiness.md` | `tests/unit/test_gmssl_provider_retry.py` (5 项：瞬态重试成功/有界失败/密码级错误不重试/OSError 重试/参数校验); `tests/e2e/test_cockpit_launcher.py` (1 项全生命周期); `tests/unit/test_supervision_meeting.py` (10 项回归); `tests/unit/test_production_support.py` (14 项含 lock path) | unit 643/643 (skipped=2)；integration 209 回归 + GmSSL 真实集成 5/5；e2e 12/12（demo 3 + return 1 + launcher 1 + 既有 7）；`make quality` exit=0 fingerprint=`6ba24930200fc687`；audit fully-sealed；安全自查 Critical/High=0/0 | done | 本次提交（工作树内，未 commit） |
| US-5 | PACKAGE-DB-1 | 已处理包登记表持久化（2026-08-03，协议 §17）：`PackageStoreDb` SQLite 持久化（显式 create/open、拒绝覆盖/隐式建库、schema_version+schema_sha256+DDL 精确比对锁定、PRAGMA integrity_check、追加式 SHA-256 哈希链 prev_hash/record_hash 全链复验、meta.record_count 一致性）；UNIQUE 约束 + BEGIN IMMEDIATE 事务实现 package_id/package_digest 重复拒绝（§17 情形 1/2，跨重启有效）；严格行校验（64 位小写 hex 摘要、ISO-8601 UTC 'Z' 时间、结果枚举 committed/rolled_back、字段大小上限、sequence_no 范围）；get/by_digest/by_scope/revision_for/iter_records/snapshot 桥接回 in-memory ProcessedPackageStore，既有 facade 兼容；仅 stdlib（sqlite3/hashlib/json/uuid/pathlib/datetime），无新依赖，不改 US-5 wire 布局与协议版本。 | `src/coevo/protocol/package_store_db.py`; `src/coevo/protocol/__init__.py` | `tests/unit/test_package_store_persistence.py` (28 项：生命周期/重复拒绝/查询与快照/异常输入/篡改检测); `tests/integration/test_package_store_persistence.py` (7 项：跨重启持久化、跨重启重复识别、篡改拒开、重开后续写) | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 643+28 / integration 209+7 / security 96 / e2e 12 全绿；verifier/protocol/security 放行（内联复核；沙箱门禁因 .tools junction=reparse point 环境性受阻，已留痕至 DECISIONS） | done | `f196bc4` |
| US-12 | AC-2 | 督办提醒与催办建议（AC-3，2026-08-03）：`ReminderKind`(REMIND/URGE) + `ReminderSuggestion`（`REMINDER_WINDOW_SEC`=24h 内提醒、逾期 URGE），`SupervisionCoordinator.coordinate` 生成提醒，`SupervisionOutcome.reminders` 校验（唯一/有序/引用已知项）与 to_dict(reminder_count)/to_audit_record(reminder_kinds) 投射；纯函数无 IO，不改 wire/协议。另含工程基线修复：`scripts/invoke-gmssl-crypto.ps1` 编译探测窗口 1s→8s + 清理删除容错（Windows Defender 对新 helper exe 暂锁约 3s 导致误报编译失败 → GCP-E-LAUNCH 抖动；修复前探针 4/8 失败→修复后 0/8），`toolchain-lock.json` launcher 哈希同步。 | `src/coevo/supervision/models.py`; `src/coevo/supervision/service.py`; `src/coevo/supervision/__init__.py`; `scripts/invoke-gmssl-crypto.ps1`; `docs/dependencies/toolchain-lock.json` | `tests/unit/test_supervision_meeting.py`（14 项：AC-1 既有 10 项 + AC-3 新增 4 项：提醒模型校验/outcome 校验/提醒分级/审计投射） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 671+4 / integration 216 / security 96 / e2e 12 全绿；verifier/protocol/security 内联放行（protocol 不涉及；security Critical/High 0） | done | `d0caac9` + `1203527` |
| US-3 | AC-2 | 人才库持久化入库（2026-08-03，AC-2 脱敏字段最小契约）：`TalentStore` SQLite 持久化（显式 create/open/from_pool、拒绝覆盖与隐式建库、schema_version+schema_sha256+DDL 精确比对锁定、PRAGMA integrity_check、追加式 SHA-256 哈希链 prev_hash/record_hash 全链复验、talent_code UNIQUE + BEGIN IMMEDIATE 事务）；严格行校验（safe-id、skill_tags/credentials 封闭格式、load 计数范围、ISO-8601 UTC 'Z' 可用窗口、identity_hash 64-hex、display_hint ≤16、identity.pool_code 必须匹配库）；snapshot 经 TalentPool 构造器复验（唯一/非空/pool 匹配，fail-closed）；导入脱敏 `talent_from_import`（raw PII → redact_identity → Talent，原始 PII 绝不落盘）；仅 stdlib 无新依赖，不改 wire/协议。 | `src/coevo/talent/store.py`; `src/coevo/talent/__init__.py` | `tests/unit/test_talent_store.py`（24 项：生命周期/注册查询/校验/导入/篡改检测）; `tests/integration/test_talent_store_persistence.py`（6 项：跨重启持久化、重复码跨重启拒绝、快照驱动推荐闭环、篡改拒开、重开续写、from_pool 回环） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 675+24 / integration 216+6 / security 96 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `71e72ac` |
| US-13 | AC-2 | 简报类型内容差异化（2026-08-03，AC-5 阶段简报/周期报告/风险专题）：`_build_content` 新增可选参数 period_start/period_end/topic_risk_ids——PERIODIC 标题与总体进展含周期窗口、RISK_TOPIC 高风险与待决仅聚焦指定风险且标题带专题标记；跨类型参数（STAGE 带 topic、PERIODIC 带 topic、RISK_TOPIC 带周期）与非法值（周期缺失/倒置、未知或重复 topic id、非 tuple/空/非字符串）全部 fail-closed；参数缺省时保持 US-13-AC-1 标签形态（向后兼容，CAS 版本链与既有测试不变）。 | `src/coevo/decision_brief/models.py`; `src/coevo/decision_brief/service.py` | `tests/unit/test_decision_brief.py`（25 项含新增 5 项：周期窗口/专题聚焦/未知与重复 topic 拒绝/周期边界与顺序/跨类型参数拒绝；既有 20 项含 CAS 版本链全部回归） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 699+5 / integration 222 / security 96 / e2e 12 全绿；security 内联放行（纯内容构建，无新信任边界）；protocol 不涉及 | done | `410c7e5` |
| US-2 | AC-2 | 用户任务编辑操作（2026-08-03，AC-6 新增/删除/修改/重排）：`src/coevo/task_decomposition/editing.py` 提供 add_task/remove_task/update_task/reorder_tasks 四个纯函数——每次编辑经 `build_baseline` 全量重校验（任务 ID 全局唯一、ISO-8601 UTC 'Z' 窗口、交付物封闭枚举、依赖环 fail-closed、里程碑重建）并返回 version+1 基线，追加 `Override` 审计记录（target_path/original/edited/reason）；remove 拒绝清空工作包；reorder 必须是精确排列；update 至少改一个字段。 | `src/coevo/task_decomposition/editing.py`; `src/coevo/task_decomposition/__init__.py` | `tests/unit/test_task_decomposition_editing.py`（8 项：add 追加/版本+1/图重建、重复与未知包拒绝、remove 一致性/清空拒绝、update 字段与空更新拒绝、reorder 排列与覆盖记录、非排列拒绝） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 704+8 / integration 222 / security 96 / e2e 12 全绿；security 内联放行（纯函数编辑，无新信任边界）；protocol 不涉及 | done | `c05823e` |
| US-2 | AC-3 | 任务分解智能体增强（2026-08-03，AC-3 方案 B DeepSeek，业务负责人批准）：统一模型适配层 `src/coevo/model/`（§9.2 可替换：`ModelProvider` Protocol + `NullModelProvider` 离线默认 + `select_provider` 环境驱动）；`DeepSeekProvider` 仅 stdlib `urllib`（无新依赖/无运行时下载），密钥仅 `COEVO_LLM_API_KEY` 环境变量且绝不进日志/repr/请求体，外发需 `COEVO_LLM_EXTERNAL_DATA_OK=1` 审批开关，超时/HTTP 错误/畸形响应全部 fail-closed；`TaskDecompositionAgent` 有界 prompt/响应（≤16KB/≤64KB）、严格 schema 校验（safe-id、ISO-Z 窗口、≤8 任务/≤16 边、未知包/未知任务拒绝）、离线返回 None 回退确定性分解；`apply` 仅产出版本+1 草稿（`model.suggestion:` Override），人工确认边界不变，候选依赖边仅建议不自动应用。 | `src/coevo/model/__init__.py`; `src/coevo/model/contract.py`; `src/coevo/model/deepseek.py`; `src/coevo/task_decomposition/agent.py`; `src/coevo/task_decomposition/editing.py`（_rebuild 泛化为多 Override） | `tests/unit/test_model_provider.py`（14 项：NullProvider/JSON 解析边界/密钥 repr 安全/缺钥与外发开关/请求构造与密钥不进 body/HTTP 错误/畸形响应/缺 choices/参数边界/选择器）; `tests/unit/test_task_decomposition_agent.py`（6 项：有效建议解析/离线返回 None/畸形与越界拒绝/未知包与未知边拒绝/apply 草稿+model override+边不自动应用/重复与未知包拒绝） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 712+20 / integration 222 / security 96 / e2e 12 全绿（门禁全程离线，provider 仅 mock 测试）；security 内联放行（Critical/High 0；Medium 观察项：真实涉密数据发往外部模型须按 §9.1 另行审批） | done | `0d7a899` |
| US-2 | AC-4 | 模型访问配置与提示词版本化（2026-08-03，用户指令）：`config/model-config.json` 承载非敏感模型配置（schema_version/provider/prompts_file/providers.deepseek：base_url/model/api_key_env 环境变量名引用/timeout_seconds/max_tokens/external_data_ok），`load_model_config` 严格校验 fail-closed（未知字段/非法 provider/路径逃逸/越界数值/非 https 全部拒绝）；`config/model-prompts.json` 版本化提示词库（每个条目 id/version/provider_key/system/user_template + SHA-256 digest，加载时复验防篡改；按 (id, provider_key) 精确解析、default 兜底；占位符 {project}/{flow} 有界展开、未知占位符拒绝）；`DeepSeekProvider` 改为 api_key_env 引用（密钥仅运行时读环境变量）；`TaskDecompositionAgent.suggest` 改由 config+prompt_registry 驱动（per-model 变体：deepseek/deepseek-chat 独立 system 提示词），方便按模型调优。 | `config/model-config.json`; `config/model-prompts.json`; `src/coevo/model/config.py`; `src/coevo/model/prompts.py`; `src/coevo/model/deepseek.py`; `src/coevo/model/__init__.py`; `src/coevo/task_decomposition/agent.py` | `tests/unit/test_model_provider.py`（22 项：含新增 ConfigLoaderTests 9 子项 + PromptRegistryTests 5 项 + DeepSeek 密钥环境名模式 8 项）; `tests/unit/test_task_decomposition_agent.py`（6 项回归，suggest 改由配置/提示词库驱动） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 732+22 / integration 222 / security 96 / e2e 12 全绿（门禁离线）；security 内联放行（密钥不进配置文件/日志/repr/请求体，提示词库 digest 防篡改，Critical/High 0） | done | `c12c8d3` |
| US-2 | AC-5 | 本地模型服务接入（2026-08-03，生产模型一版 = 本地 vLLM/llama.cpp，用户指令）：`src/coevo/model/openai_compatible.py` 统一 OpenAI 兼容 provider（vLLM/llama.cpp/DeepSeek 通用）——回环 host（127.0.0.1/localhost/::1）自动判定**本地模式**：免 API 密钥、免 external_data_ok 外发审批、数据不出机（§9.1 本地部署合规）；非回环必须 https + 密钥（api_key_env）+ external_data_ok（fail-closed 外发门禁）；端点自动归一 `/v1/chat/completions`（base_url 缺 /v1 自动补）；`config/model-config.json` 新增 `local_openai` 档案（默认 http://127.0.0.1:8000/v1、model 占位、timeout 60s）；`DeepSeekProvider` 收敛为远程子类（向后兼容）；provider_key 仍按 name/model 解析提示词变体。 | `src/coevo/model/openai_compatible.py`; `src/coevo/model/deepseek.py`; `src/coevo/model/config.py`; `src/coevo/model/__init__.py`; `config/model-config.json` | `tests/unit/test_model_provider.py`（29 项：新增 LocalProviderTests 4 项：本地免钥免外发/端点归一/远程 http 拒绝/远程仍需钥与外发；config local_openai 加载、回环 http 允许、缺失 provider 条目拒绝；选择器 local_openai） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 732+29 / integration 222 / security 96 / e2e 12 全绿（门禁离线）；security 内联放行（本地模式数据不出机，Critical/High 0） | done | `317e080` |
| ENG-BASE | INSTALL-1 | 离线安装/升级/回滚/卸载工具（2026-08-03，P0-1 决策点，用户指令）：`scripts/install_cockpit.py` 纯 stdlib——版本化 `app/<version>` 安装目录（src/scripts/run_cockpit.py/scripts/run_demo.py/config/README.md 固定包内容，跳过符号链接与 __pycache__）、每版本 SHA-256 完整性清单（`manifests/<version>.sha256`，复制即哈希、写清单后复验、`current` 指针原子切换）、`releases.json` 安装历史（schema 1.0，previous 链）、升级保留上一版本、回滚先复验上一版本清单再切指针、卸载仅移除当前版本（数据/日志与其他版本保留）、`check` 校验当前安装（忽略运行时 __pycache__）、单实例锁（10 分钟陈旧接管）；fail-closed：版本号 `^\d+\.\d+\.\d+$` 限安全路径段（禁时间戳）、安装失败清理目标、install_root 不得等于 source_root、破坏范围限 app/manifests/releases.json/current；`docs/operations/install-upgrade.md` 运维手册（含已知限制：清单未签名、无 pyproject 打包、无服务自启）。 | `scripts/install_cockpit.py`; `docs/operations/install-upgrade.md` | `tests/integration/test_installer.py`（11 项：安装布局/check/升级保留上一版/回滚/回滚无前版 fail-closed/卸载当前/卸载全部/非法版本/非法源/重复版本 force/损坏检测） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 768 / integration 236 / security 97 / e2e 12 全绿；冒烟：安装 0.2.0 → 已安装 run_cockpit --check ok → install check ok；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `9331d34` |
| ENG-BASE | AUDIT-KEY-1 | 审计签名密钥健康诊断与恢复手册（2026-08-03，P0-3 决策点本地部分，用户指令）：`scripts/audit_key_health.py` 纯 stdlib 健康诊断——config.structure（字段/thumbprint/哈希/算法）、config.public_certificate（公钥文件存在且 SHA-256 匹配、不越出仓库根）、config.head_signer（链头签名者=配置或存在 `audit-signing-<THUMB>.json` 历史归档且匹配）、certificate.inspect（委托 `audit_signature.ps1 -Action Inspect`：CurrentUser/My 恰好 1 个、有私钥、非导出、有效期内）；结构化 JSON 输出 + 处置建议（remediations），退出码 0/1；全程不读取不打印私钥材料；`docs/operations/audit-key-runbook.md` 按故障类型（证书丢失/配置损坏/可导出私钥/历史归档缺失）恢复与轮换流程 + 归档纪律 + 升级路径。 | `scripts/audit_key_health.py`; `docs/operations/audit-key-runbook.md` | `tests/unit/test_audit_key_health.py`（20 项：配置加载/结构校验/公钥文件/链头签名者归档/处置建议/报告构建）; `tests/integration/test_audit_key_health.py`（4 项：真实仓库健康 exit0/仅配置检查/损坏配置 fail-closed/缺失配置 fail-closed） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 790 / integration 240 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `e3ab4aa` |
| ENG-BASE | DOCS-COMMENT-1 | 文档与代码注释规范化（2026-08-03，用户指令：规范文档和代码注释，向生产落地靠近）：① 新增 `docs/operations/configuration-reference.md` 权威 `COEVO_*` 环境变量登记表（运行时/模型/工具链/仅测试四类，含默认值与校验规则，`config/model-config.json` 与 `model-prompts.json` 边界）；② 修正 4 个模块过时注释（knowledge_base/cockpit/talent/task_decomposition 中 "deferred to US-7-AC-2/3/4"、"deferred to US-14-AC-2/AC-4"（AC-4 不存在）、"future slice" 等已完成切片描述 → 落到实际实现模块 store.py/server.py/agent.py）；③ `docs/README.md` 索引补齐 process/operations/plans/production-readiness/audit-signing 并新增生产运维文档入口；④ `README.md` 增生产部署小节（安装/升级/回滚/启动 + 文档导航）；⑤ `docs/production-readiness.md` 指向配置参考；⑥ `docs/development-environment.md` 增"代码注释与文档规范"（注释与实现一致/边界落到实现模块/why-not-code/语言一致性/配置登记纪律/追溯纪律）；⑦ `tests/unit/test_production_docs.py` 3 项门禁：config.py 变量全登记、登记变量均有使用（含 .ps1/.cs）、已完成模块无过时注释。 | `src/coevo/knowledge_base/__init__.py`; `src/coevo/cockpit/__init__.py`; `src/coevo/talent/__init__.py`; `src/coevo/talent/service.py`; `src/coevo/task_decomposition/__init__.py`; `src/coevo/task_decomposition/service.py`; `docs/operations/configuration-reference.md`; `docs/README.md`; `README.md`; `docs/production-readiness.md`; `docs/development-environment.md` | `tests/unit/test_production_docs.py`（3 项：runtime 变量全登记/登记变量均有使用/已完成模块无过时注释）; 回归 `tests/unit/test_knowledge_base.py`; `tests/unit/test_cockpit.py`; `tests/unit/test_cockpit_http.py`; `tests/unit/test_talent_recommender.py`; `tests/unit/test_talent_store.py`; `tests/unit/test_task_decomposition.py`; `tests/unit/test_task_decomposition_agent.py`; `tests/unit/test_task_decomposition_editing.py` | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 793 / integration 240 / security 97 / e2e 12 全绿；security 内联放行（注释/文档变更，无行为改动，Critical/High 0）；protocol 不涉及 | done | 待提交 |
| ENG-BASE | CRYPTO-1 | 正式密码方案落地为功能可用版本（2026-08-03，用户批准：做一个简单功能可用版本或使用开源第三方包库 → 采用开源 GmSSL 3.2.0（Apache-2.0）+ 纯 Python SM3）：① 新增 `src/coevo/crypto/sm3.py` 纯 Python SM3（GB/T 32905）——官方向量（"abc"/空串）+ 与 GmSSL 引擎交叉验证（integration 覆盖 0/3/55/56/64/1000 字节边界）；② `compute_sm3_digest` 由 SHA-256 替身切换为真实 SM3（协议摘要与声明的 `CS-SM2-SM4-AEAD-SM3-01` 一致，收发双方一致使用）；③ 用户批准开源引擎为功能路径：`crypto/contract.py` scope 治理说明、`toolchain-lock.json` scope 文本、`approved-crypto-provider-path.md` §1、README 交付边界、`audit-signing.json` formal_replacement 同步；④ 受保护密钥句柄与国密认证仍为 APPROVED_PRODUCT scope 硬性前置（`require_approved` 不变），属长期目标。 | `src/coevo/crypto/sm3.py`（新）; `src/coevo/crypto/__init__.py`; `src/coevo/crypto/contract.py`; `src/coevo/protocol/sm2_sign.py`; `src/coevo/protocol/__init__.py`; `src/coevo/report/models.py`; `docs/dependencies/toolchain-lock.json`; `docs/dependencies/approved-crypto-provider-path.md`; `README.md`; `loop/audit-signing.json` | `tests/unit/test_crypto_sm3.py`（7 项：官方向量/边界 55·56·63·64·65·127·128/确定性/空串/digest-hex 一致/类型拒绝）; `tests/integration/test_crypto_sm3.py`（2 项：纯 SM3 vs GmSSL 引擎交叉验证/官方向量）; 回归 `tests/integration/test_agent_package_aead.py`; `tests/integration/test_agent_package_atomic_import.py`; `tests/integration/test_gmssl_prototype_provider.py`（scope 断言同步）; `tests/e2e/test_return_chain.py` | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 800 / integration 242 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 内联放行（无 wire 布局/版本改动，摘要算法与声明套件对齐） | done | `69fe4c2` |
| ENG-BASE | CI-1 | CI 与制品托管方案实施（2026-08-03，用户批准 CI/制品托管方案）：① `.github/workflows/quality.yml`——Windows runner，workflow_dispatch/push(PR) 触发，restore 锁工具链制品后运行 `fmt/lint/test/test-security/test-e2e` 四个验证侧目标并上传门禁证据；`--target quality` 的签名封存保留维护机（审计签名私钥不可导出、不能上 runner），runner 仅用公钥复验既有审计链；② `scripts/ci-restore-toolchain.ps1`——https 下载（或 `-LocalPath` 测试注入）、SHA-256 锚定 fail-closed（不匹配不解压）、先临时解压校验 `python.exe/node.exe/control.pyz` 存在且仓库 `toolchain-lock.json` 可解析再复制进 `.tools/`、清理不留半成品；③ `docs/dependencies/ci-artifact.json` 制品描述符（sha256=pending 时拒绝运行）；④ `docs/operations/ci-artifact-hosting.md` 托管方案（构建/发布/回填哈希/激活步骤/升级/安全说明）。激活前置：所有者推送工作流并发布制品（当前仓库规则禁 push，已记录）。 | `.github/workflows/quality.yml`（新）; `scripts/ci-restore-toolchain.ps1`（新）; `docs/dependencies/ci-artifact.json`（新）; `docs/operations/ci-artifact-hosting.md`（新） | `tests/unit/test_ci_restore.py`（6 项：成功恢复/哈希不匹配 fail-closed/缺条目 fail-closed/pending 描述符 fail-closed/工作流四目标且不含 quality/描述符结构与 pending） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 806 / integration 242 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0，制品哈希锚定/无密钥/只读权限）；protocol 不涉及 | done | `53cad86` |
| ENG-BASE | HANDLE-1 | 受保护密钥句柄（CNG 非导出 KEK 封装 SM2 私钥，2026-08-03，用户指令：继续 国密认证模块 + 受保护密钥句柄）：① `scripts/cng-kek.ps1` 受控 CNG 助手——创建/状态/封装/解封摘要/销毁非导出 RSA-2048 KEK（`ExportPolicy=None`、KeyUsage=Decryption、OAEP-SHA256、输入缓冲归零、输出仅密文与摘要、KEK 名正则 `CoevoSm2Kek-<32hex>`）；② `src/coevo/crypto/cng_handle.py`——`CngKekReference`（仅元数据）/`CngKekStore`（helper 哈希钉扎 6118 字节）/`CngProtectedKeyHandle`（实现 ProtectedKeyHandle 协议，SM2 OID `1.2.156.10197.1.301`）/`CngWrappedKeyRegistry`（JSON + SHA-256 哈希链、显式 create/open、篡改拒开、注册/撤销/销毁生命周期、快照、明文永不落盘）；③ `src/coevo/crypto/protected_provider.py`——`GmsslProtectedProvider`（`key_handle_backed=True`、scope=`APPROVED_PRODUCT`，`require_approved` 可放行；`sm3`/`seal`/`verify` 公钥侧功能可用；`sign`/`open` 需 HANDLE-2 helper 解封动作，fail-closed 不回落原型路径）；④ 文档：`key_handle.py` 契约说明 + `approved-crypto-provider-path.md` §8 状态（HANDLE-1 完成 / HANDLE-2 剩余：helper 内解封+SM2 签名动作 / 国密认证模块外部采购）。 | `scripts/cng-kek.ps1`（新）; `src/coevo/crypto/cng_handle.py`（新）; `src/coevo/crypto/protected_provider.py`（新）; `src/coevo/crypto/__init__.py`; `src/coevo/crypto/key_handle.py`; `docs/dependencies/approved-crypto-provider-path.md` | `tests/unit/test_cng_handle.py`（12 项：KEK 引用校验/受保护句柄/注册表 create-open/注册-撤销-销毁/篡改拒开/重开持久化/provider 契约与 HANDLE-2 fail-closed/公钥侧委托/类型校验）; `tests/integration/test_cng_handle.py`（4 项：真实 CNG 创建-状态-封装-解封摘要-销毁/重开句柄封装回环/注册表持久化） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 818 / integration 246 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `cb902b2` |
| ENG-BASE | HANDLE-2 | crypto-helper 内 CNG 解封 + SM2 签名/解包（2026-08-03，用户指令，HANDLE-1 接线）：① `scripts/gmssl-crypto-helper.cs` 动作 6/7/8——动作 8 用 `RSACng`+非导出 KEK 封装 SM2 密钥的 PKCS#8 口令（RSA-2048 OAEP-SHA256 上限 190B，封装整包 564B 不可行 → 封装口令、密钥保持口令加密于 profile，设计说明写入 §8）；动作 6（签名）解封口令→`sm2_private_key_info_decrypt_from_der` 解密→SM2 签名；动作 7（解包）解封口令→解包 SM4-GCM；口令/密钥在 helper 内存使用后归零，Python 全程不可见；helper 增 `System.Core` 引用（RSACng）并透传完整 GCP-E 诊断（launcher 不再截断）；② `src/coevo/crypto/gmssl_provider.py` 增 `sign_wrapped/open_wrapped/protect_key`（帧上限 8，role 帧）；③ `GmsslProtectedProvider.sign/open` 功能化（构造增 role/wrapped/kek_ref/profile）；④ 文档同步（cng_handle/key_handle/approved-crypto-provider-path §8）。 | `scripts/gmssl-crypto-helper.cs`; `scripts/invoke-gmssl-crypto.ps1`; `docs/dependencies/toolchain-lock.json`（helper 源哈希 + System.Core 引用 + launcher 哈希）; `src/coevo/crypto/gmssl_provider.py`; `src/coevo/crypto/protected_provider.py`; `src/coevo/crypto/cng_handle.py`; `src/coevo/crypto/key_handle.py`; `docs/dependencies/approved-crypto-provider-path.md` | `tests/unit/test_cng_handle.py`（sign/open 委托与构造校验更新）; `tests/integration/test_cng_handle.py`::ProtectedSm2RoundTripTests（3 项：受保护签名+公钥验签/受保护解包+篡改拒绝/错误 KEK fail-closed）; 回归 `tests/integration/test_gmssl_prototype_provider.py`（锁测试）; `tests/unit/test_crypto_sm3.py` | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 818 / integration 249 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `e1fb9af` |
| ENG-BASE | OPS-1 | 生产运维与可观测性落地（2026-08-04，用户指令：继续生产落地优化）：① `scripts/health_check.py` 离线健康检查（stdlib、JSON 输出、退出码 0=ok/1=degraded/2=critical）——数据/日志目录存在且可写、磁盘余量 ≥ `--min-free-bytes`（默认 512MiB）、驾驶舱 `/healthz`（默认 127.0.0.1:12701，不可达=degraded）、审计封存状态（`audit_seal.py verify`：fully-sealed ok / 未封尾 degraded / 失败 critical）、安装版本一致性（current 指针与安装包 version.py 一致）、单实例锁陈旧（<10 分钟）；全程只读；② `scripts/register-autostart.ps1` Windows 计划任务登录自启已安装驾驶舱（Register/Unregister/Status；`-DryRun` 不触系统；schtasks EAP=Continue 兼容 PS5.1；失败关闭：安装根/指针/runner/Python 缺失即中止）；③ `src/coevo/cockpit/server.py` `_CockpitLogWriter` 访问日志按大小轮转（默认 5MB×5、备份移位、轮转失败计入 errors 且不影响请求处理）；④ `docs/operations/ops-runbook.md` 统一运维手册（健康检查/自启/轮转/备份恢复最小集合/排障速查/边界）；⑤ `docs/README.md` operations 索引更新。 | `scripts/health_check.py`（新）; `scripts/register-autostart.ps1`（新）; `src/coevo/cockpit/server.py`; `docs/operations/ops-runbook.md`（新）; `docs/README.md` | `tests/unit/test_ops_tooling.py`（10 项：dirs/disk/version/lock/聚合、自启 Status 未注册与 DryRun 打印、缺安装根失败关闭、日志按大小轮转与备份移位、轮转失败写隔离）; 回归 `tests/unit/test_cockpit_http.py`; `tests/integration/test_cockpit_http_server.py`; `tests/e2e/test_cockpit_launcher.py`（50 项） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 828 / integration 249 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `2d1fb28` |
| ENG-BASE | BACKUP-1 | 备份/恢复工具与恢复故障注入测试（2026-08-04，用户指令：继续生产落地优化）：① `scripts/backup_state.py`（stdlib）——backup/verify/restore/list 四动作；备份驾驶舱状态（cockpit-state/access.jsonl/current/releases.json/wrapped-keys.json/manifests）+ 审计链（loop/tool-audit.jsonl、audit-head*、audit-signing*、audit-checkpoint）；SHA-256 清单原子写入、verify 逐文件复验、restore 先验后恢复、运行中新鲜锁拒绝恢复、恢复前 `.pre-restore-<ts>` 备拷、路径穿越守卫（相对路径 + resolve 限安装根）、label 白名单；② `tests/unit/test_backup_state.py` 6 项（备份-校验-恢复回环/篡改检测/运行锁拒绝/路径守卫/label 校验/列表/重名拒绝）；③ `tests/integration/test_recovery_faults.py` 3 项——状态存储中断保存（陈旧 tmp 不破坏已提交状态、重启加载最后提交态）、安装器升级中断（app/<new> 半拷贝但指针不变、check 仍通过、无 --force 拒绝、--force 恢复完成）。 | `scripts/backup_state.py`（新）; `docs/operations/ops-runbook.md` | `tests/unit/test_backup_state.py`（6 项）; `tests/integration/test_recovery_faults.py`（3 项） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 834 / integration 252 / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `16c5f0e` |
| ENG-BASE | AVAIL-1 | 可用性落地（2026-08-04，用户指令：继续生产落地优化）：① `scripts/run_cockpit.py --preflight` fail-fast 启动预检——配置（AppConfig）+ 数据/日志目录可写 + 磁盘余量（≥256MiB）+ 审计封存状态（`audit_seal.py verify`：fully-sealed ok / 未封尾 warning / 失败 critical）+ 模型配置可加载（失败仅 warning，离线兜底）；退出码 0=ok/1=degraded/2=critical（critical 阻断启动）；② `scripts/cockpit-watchdog.ps1` 看门狗——轮询 `http://127.0.0.1:<Port>/healthz`，连续 `MissThreshold`（默认 3）次失败后用受控命令隐藏窗口重启已安装驾驶舱，`RestartCooldownSeconds`（默认 60）防崩溃循环；`-DryRun` 单轮探测并打印动作、不触系统；失败关闭（安装根/指针/runner/python 缺失即中止）；③ `docs/operations/ops-runbook.md` §2.1 预检与看门狗用法。 | `scripts/run_cockpit.py`; `scripts/cockpit-watchdog.ps1`（新）; `docs/operations/ops-runbook.md` | `tests/unit/test_preflight_watchdog.py`（6 项：预检 ok/不可写目录 critical/audit 失败 critical/未封尾 degraded；看门狗 DryRun 报告重启不触系统/缺安装根失败关闭）; `tests/e2e/test_cockpit_launcher.py`（新增预检健康仓库退出 0） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 840 / integration 252 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `b5b3088` |
| ENG-BASE | METRICS-1 | 驾驶舱进程内健康/状态端点（2026-08-04，用户指令：继续生产落地优化）：`GET /api/health`（需会话令牌、认证只读）返回 service/version/started_at/uptime_sec/session_count/request_count/audit_records/log_errors；请求计数线程安全（lock 保护）；未认证 401、无敏感数据；与外部 `health_check.py` 互补（进程内运行实例视图）。 | `src/coevo/cockpit/server.py`; `docs/operations/ops-runbook.md` | `tests/integration/test_cockpit_http_server.py`（新增 2 项：无令牌 401、带令牌 200 与字段断言） | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 840 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读/认证/无敏感数据）；protocol 不涉及 | done | `cc9af66` |
| ENG-BASE | SECSCAN-1 | 密钥/敏感信息扫描门禁（2026-08-04，用户指令：继续生产落地优化）：① `scripts/secret_scan.py` 扫描已跟踪文本文件——私钥块（PEM）、AWS `AKIA`、GitHub `ghp_`、OpenAI `sk-`、Slack `xox`、高熵 key/secret/token/password 赋值（≥20 字符引号值）；`tests/` 允许假 PEM 与假密钥赋值夹具（令牌格式仍全路径拦截）；扫描器跳过自身源码；非 git 根目录回退递归扫描（供测试）；逐文件 ≤1MiB；② `quality_gate.py` lint 目标新增 secret_scan 步骤（命中即门禁失败）；③ 锁链同步（quality_gate.py 哈希/python-script-lock.tsv/toolchain-lock script_inventory/make.cs ScriptInventorySha256 与源哈希）；④ docs/development-environment.md 门禁说明。 | `scripts/secret_scan.py`（新）; `scripts/quality_gate.py`; `docs/dependencies/python-script-lock.tsv`; `docs/dependencies/toolchain-lock.json`; `scripts/tool-shims/make.cs`; `docs/development-environment.md` | `tests/unit/test_secret_scan.py`（6 项：仓库清洁/库外 PEM 检出/tests PEM 放行/令牌全路径拦截/key 赋值检出/退出码）; 回归 `tests/integration/test_dev_environment_entry.py` + `tests/security/test_local_toolchain_security.py`（20 项，锁链一致） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`（lint 命令集新增 secret_scan 后的新基线）；audit fully-sealed；unit 846 / integration 252 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `6728edd` |
| ENG-BASE | RELEASE-1 | 发布就绪检查与已知限制清单（2026-08-04，用户指令：继续生产落地优化）：① `scripts/release_check.py`（stdlib，JSON + 退出码 0=就绪/1=警告/2=不发布）——git 工作区干净、版本语义化且与 `--expect-version` 一致、审计 fully-sealed（未封尾 warning）、secret_scan 干净、追溯矩阵一致、loop/STATE done 且无阻塞、无 in-progress（ready=warning 显式推迟）；② `docs/operations/known-limitations.md` 已知限制清单——外部条件（国密认证模块/Win7 实机/CI 激活/审计密钥托管/Windows 服务形态）与实现边界（看门狗依赖 PATH python/备份默认本地/secret_scan 启发式/request_count 认证请求/轮转明文/模型外发开关语义/原型密码路径）+ 维护注意（门禁指纹随命令集变化、锁链全链同步、COEVO_* 登记）；③ ops-runbook §7 发布就绪节。 | `scripts/release_check.py`（新）; `docs/operations/known-limitations.md`（新）; `docs/operations/ops-runbook.md` | `tests/unit/test_release_check.py`（5 项：版本/状态/backlog/子进程与聚合/退出码，子进程 mock） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 851 / integration 252 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0）；protocol 不涉及 | done | `2c1cb50` |
| ENG-BASE | REVIEW-FIX-1 | 修复 2026-08-03 全面审查发现的代码级问题（2026-08-03，用户指令）：① `import_service` 删除占位 pass，fixed-header 三长度与真实块一致性校验 fail-closed，无显式 base/current revision 时拒绝导入（不再默认 R0001 伪造主版本，协议 §16）；② `merge._reject` 畸形 import record 抛 MergeError，不再静默伪造 decision_maker；③ `report._one_year_after` 无效时间戳抛 ReportBuilderError；④ `workspace/paths` `..` 穿越校验同时覆盖 POSIX 与 Windows 反斜杠方言（PurePosixPath+PureWindowsPath）；⑤ `identity._cleanup_failed_create` 清理失败记 warning 且绝不掩盖原始 create 错误；⑥ `package_builder` 两个构建器统一预填 FixedHeader 长度（内存不变量与 to_bytes 一致），`build_signed_payload` 更名 `assert_sign_blocked` 并澄清 fail-closed 语义；⑦ cockpit 状态周期快照（默认 300s，`COEVO_COCKPIT_CHECKPOINT_SEC` 可配，stop 时 join+最终落盘）解决崩溃丢状态；⑧ OpenAI 兼容 provider 响应体硬上限 4MiB + 按 max_tokens 软上限、连接类瞬时失败有界重试 1 次（HTTP 错误/超限不重试）；⑨ cockpit 并发有界（默认 16，饱和 503）。仅 stdlib，零新增依赖，不改 wire/协议版本/密码方案。 | `src/coevo/protocol/import_service.py`; `src/coevo/protocol/package_builder.py`; `src/coevo/protocol/__init__.py`; `src/coevo/merge/engine.py`; `src/coevo/report/builder.py`; `src/coevo/workspace/paths.py`; `src/coevo/identity/repository.py`; `src/coevo/cockpit/server.py`; `src/coevo/model/openai_compatible.py`; `src/coevo/config.py`; `scripts/run_cockpit.py`; `docs/production-readiness.md` | `tests/unit/test_model_provider.py`（含连接重试/超限拒绝 4 项）; `tests/unit/test_report_builder.py`; `tests/unit/test_workspace_init.py`; `tests/unit/test_merge_engine_v3.py`; `tests/unit/test_cockpit_http.py`; `tests/unit/test_production_support.py`; `tests/unit/test_protocol_sign_blocked.py`（新）; `tests/integration/test_agent_package_atomic_import.py`; `tests/integration/test_cockpit_http_server.py`; `tests/integration/test_cockpit_state_persistence.py`; `tests/security/test_identity_store_security.py`; `tests/e2e/test_return_chain.py` | `make quality` exit=0 fingerprint=`34fc0b672c25a7b5`；audit fully-sealed；unit 759+ / integration 225+ / security 97 / e2e 12 全绿；security 内联放行（Critical/High 0）；protocol 不涉及（无 wire 改动） | done | `50513a7` |
| ENG-BASE | LOAD-1 | 驾驶舱 HTTP 并发/延迟容量探针（2026-08-04，用户指令：继续生产落地优化）：`scripts/benchmark.py` 新增 `_cockpit_http_probe`——起真实环回 cockpit，16 worker × 8 = 128 次 `GET /healthz`，报告 p95/p50/max 与错误数；载荷对齐生产并发上限（`max_concurrent_requests=16` 默认值），每请求新建连接（贴合服务端 one-request-per-connection 生产行为）；SLA p95≤1.0s 且 errors=0（`COCKPIT_HTTP_P95_LIMIT_SEC` 参数化；参考架构普通页面 3s，实测 p95≈0.52s 留近一倍余量）；`tests/unit/test_benchmark_http.py` 2 项（零错误且 ok / 延迟边界绑定 result.limit）。 | `scripts/benchmark.py` | `tests/unit/test_benchmark_http.py`（2 项） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 855 / integration 252 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读探针/并发有界/无敏感数据）；protocol 不涉及 | done | `dce89c2` |
| ENG-BASE | OPS-2 | 固化看门狗/自启的显式 Python 解释器路径（2026-08-04，用户指令：继续生产落地优化）：① `scripts/register-autostart.ps1` 新增 `PinPython` 动作——写 `<install_root>\python-path.txt`（不碰计划任务，供刷新陈旧 pin）；`Register` 在创建计划任务前固化解析到的解释器（显式 `-PythonPath` 或 PATH），写失败即中止；② `scripts/cockpit-watchdog.ps1` 新增 `-PythonPath`，解析顺序：显式参数 → sidecar → PATH；sidecar 空/相对路径/指向缺失解释器均失败关闭（不静默回退）；③ `scripts/install_cockpit.py` 安装/升级成功时原子写入 `sys.executable`（指针切换前，失败即中止安装）；④ 文档：ops-runbook §2/§2.1 + known-limitations 条目更新。 | `scripts/register-autostart.ps1`; `scripts/cockpit-watchdog.ps1`; `scripts/install_cockpit.py`; `docs/operations/ops-runbook.md`; `docs/operations/known-limitations.md` | `tests/unit/test_ops_tooling.py`（+4 项：PinPython 写侧车/干跑不写/缺安装根失败关闭/Register 干跑打印 pin 与任务）; `tests/unit/test_preflight_watchdog.py`（+4 项：sidecar 生效/指向缺失解释器失败关闭/相对路径失败关闭/显式路径缺失失败关闭）; `tests/integration/test_installer.py`（+1 项：安装固化解释器） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 863 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读 sidecar/无新信任边界/无新增依赖）；protocol 不涉及 | done | `46b2df1` |
| ENG-BASE | BACKUP-2 | 备份异卷保证（2026-08-04，用户指令：继续生产落地优化）：`scripts/backup_state.py` 新增 `--require-external`——备份根位于安装根内、或与安装根同卷（`st_dev` 比较）时失败关闭，防止磁盘故障同时毁掉数据与备份；manifest 增加 `same_volume` 机器可读字段供自动化核查；备份根非目录、不可写、目标不可创建时干净报错（不再裸 traceback）；`--require-external` 仅作用于 backup 动作，verify/restore 不受限；文档：ops-runbook §4 异地备份示例 + known-limitations 备份条目更新。 | `scripts/backup_state.py`; `docs/operations/ops-runbook.md`; `docs/operations/known-limitations.md` | `tests/unit/test_backup_state.py`（+4 项：外部根工作且 same_volume 标记/安装根内拒绝/同卷拒绝（断言消息）/文件作备份根干净失败） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 867 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，纯 stat/无敏感数据/无新增依赖）；protocol 不涉及 | done | `2b140a2` |
| ENG-BASE | SECSCAN-2 | 密钥扫描模式扩展（2026-08-04，用户指令：继续生产落地优化）：`scripts/secret_scan.py` ① `pem_private_key` 覆盖 `SM2 PRIVATE KEY`（国密格式，tests/ PEM 放行语义不变）；② `github_pat` 扩展为令牌家族 `ghp_`/`gho_`/`ghu_`/`ghs_`/`ghr_`/`github_pat_`（fine-grained）；③ 新增 `google_api_key`（`AIza`+35）与 `npm_token`（`npm_`+36）；④ 全部高置信格式、令牌类全路径拦截（含 tests/）；lint 门禁自动获益（secret_scan 步骤）。 | `scripts/secret_scan.py` | `tests/unit/test_secret_scan.py`（+5 项：SM2 检出/SM2 放行/GitHub 家族 6 前缀/Google/npm；仓库假阳性 0） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 872 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，仅加强门禁/无新信任边界/无新增依赖）；protocol 不涉及 | done | `0403eb5` |
| ENG-BASE | OPS-3 | 健康检查增加备份新鲜度监控（2026-08-04，用户指令：继续生产落地优化）：`scripts/health_check.py` 新增 `backup` 检查——扫描 `--backup-root`（默认 `<install-root>\backups`）下全部备份 manifest，取最新 `created_at`，年龄 ≤ `--max-backup-age-days`（默认 7 天）为 ok；备份根缺失/无有效 manifest/过期/未来时间戳（>1 天）均为 degraded（恢复姿态告警，不阻断服务，不误报 critical）；CLI 新增 `--backup-root` 与 `--max-backup-age-days`（<1 拒绝）；只读、纯 stdlib；docs/operations/ops-runbook.md §1 检查表 + 异地备份监控示例。 | `scripts/health_check.py`; `docs/operations/ops-runbook.md` | `tests/unit/test_ops_tooling.py`（+5 项：备份根缺失 degraded/新鲜 ok/过期 degraded 断言消息/多备份取最新/未来时间戳 degraded；build_report 含 backup 检查） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 877 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读 manifest 解析/无敏感数据/无新增依赖）；protocol 不涉及 | done | `cde90e5` |
| ENG-BASE | OPS-4 | 模型外发审批可观测性（2026-08-04，用户指令：继续生产落地优化）：`scripts/run_cockpit.py` 新增 `model_egress_warnings()`——激活 provider 非回环（https）且 `external_data_ok=true`（数据真会离开机器）或遗留开关 `COEVO_LLM_EXTERNAL_DATA_OK=1` 被设置时输出告警；`--preflight` 集成（外发获批/遗留开关 → degraded exit 1；回环 provider 与 offline 静默，数据不出机不误报）；每次启动经 `setup_logging` 写 `model egress posture` 告警日志；审批机制本身 fail-closed 不变；文档：configuration-reference 变量行 + ops-runbook §2.1 外发姿态节。 | `scripts/run_cockpit.py`; `docs/operations/configuration-reference.md`; `docs/operations/ops-runbook.md` | `tests/unit/test_preflight_watchdog.py`（+5 项：非回环获批 degraded/回环获批 ok/遗留开关 degraded/告警消息内容/offline+回环静默） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 882 / integration 254 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读配置/无敏感数据/无新增依赖）；protocol 不涉及 | done | `0d05ed3` |
| ENG-BASE | OPS-5 | 解释器 pin 完整性检查（2026-08-04，用户指令：继续生产落地优化）：`scripts/install_cockpit.py --action check` 新增 pin 校验——`python-path.txt` 缺失/空/非绝对路径/目标不存在均 check 失败（exit 1）并给出 `register-autostart.ps1 -Action PinPython` 指引，闭环 OPS-2 旧安装缺口（否则看门狗静默回退 PATH）；`scripts/health_check.py` 新增 `pin` 检查——缺失/无效 → degraded（监控侧可见性），接入 `build_report`；文档：ops-runbook §1 检查表 pin 行 + install check 强制说明、known-limitations OPS-2 条目更新。 | `scripts/install_cockpit.py`; `scripts/health_check.py`; `docs/operations/ops-runbook.md`; `docs/operations/known-limitations.md` | `tests/integration/test_installer.py`（+3 项：缺 pin/相对 pin/目标缺失 check 失败）; `tests/unit/test_ops_tooling.py`（+4 项：pin ok/缺失 degraded/相对 degraded/目标缺失 degraded；build_report 含 pin 检查） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 886 / integration 257 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，只读 pin 文件/无敏感数据/无新增依赖）；protocol 不涉及 | done | `dc18853` |
| ENG-BASE | METRICS-2 | /healthz 探针计数（2026-08-04，用户指令：继续生产落地优化）：`src/coevo/cockpit/server.py` 新增 `_probe_count`（复用 `_request_lock` 线程安全）——`/healthz` 存活探测独立计数，与认证 `request_count` 互不混淆（METRICS-1 语义不变）；`/api/health` 响应增加 `probe_count` 字段，运维可区分看门狗/健康检查/基准探针流量与真实使用；文档：ops-runbook §1 进程内端点说明 + known-limitations request_count 条目更新。 | `src/coevo/cockpit/server.py`; `docs/operations/ops-runbook.md`; `docs/operations/known-limitations.md` | `tests/integration/test_cockpit_http_server.py`（+1 项：healthz 探测计入 probe_count 且不计入 request_count，两次读取对比断言） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 886 / integration 258 / security 97 / e2e 13 全绿；security 内联放行（Critical/High 0，仅加计数/无敏感数据/无新增依赖）；protocol 不涉及 | done | `74ffa11` |
| ENG-BASE | REVIEW-FIX-2 | 生产就绪复审修复（2026-08-04，用户指令：继续生产落地优化）：① `scripts/run_cockpit.py` 新增 `--print-token`——启动后经 `session_manager.create()` 签发一次会话令牌打印到 stdout（flush 即时可见；不进日志框架、不落盘，服务端仅存 SHA-256 摘要），闭环生产环境"认证接口无令牌签发路径"的缺口；ops-runbook §2.2 交互式访问说明；`static/app.js` 无令牌提示指向 `--print-token`（无 URL 字面量，保持离线资产无外链不变量）；② `src/coevo/cockpit/server.py` 并发饱和 503 可观测——新增 `rejected_count`（线程安全）暴露于 `/api/health`，`_reject_busy` 写有界 `busy_rejected` 访问日志行（仅 client_host/reason，不读请求内容）。 | `scripts/run_cockpit.py`; `src/coevo/cockpit/server.py`; `src/coevo/cockpit/static/app.js`; `docs/operations/ops-runbook.md` | `tests/e2e/test_cockpit_launcher.py`（+1 项：--print-token 签发→/api/health 鉴权 200→令牌不进访问日志→优雅退出）; `tests/integration/test_cockpit_http_server.py`（+1 项 busy_rejected 日志行断言；rejected_count 断言与 /api/health 字段） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 886 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，令牌仅 stdout 一次/服务端只存摘要/拒绝行无请求内容/无新增依赖）；protocol 不涉及 | done | `07cbc9d` |
| ENG-BASE | SECSCAN-3 | 密钥扫描模式再扩展（2026-08-04，用户指令：继续生产落地优化）：`scripts/secret_scan.py` ① 新增 `stripe_key`（`sk_live_`/`sk_test_`/`rk_live_`+16+，Stripe 真实/测试/受限密钥）；② 新增 `sendgrid_key`（`SG.<22>.<20>`）；③ 新增 `pgp_private_key`（`PGP PRIVATE KEY BLOCK`，并入 tests/ PEM 放行语义）；④ 全部高置信格式、令牌类全路径拦截（含 tests/）；lint 门禁 secret_scan 步骤自动获益。 | `scripts/secret_scan.py` | `tests/unit/test_secret_scan.py`（+4 项：Stripe 3 前缀检出/SendGrid 检出/PGP 库外检出/PGP tests 放行；仓库假阳性 0） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 890 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，仅加强门禁/无新信任边界/无新增依赖）；protocol 不涉及 | done | `54b5b18` |
| ENG-BASE | AVAIL-2 | 健康探测响应身份校验（2026-08-04，用户指令：继续生产落地优化）：`scripts/health_check.py` `check_cockpit`——`/healthz` 除 HTTP 200 外校验响应体 `service=coevo-cockpit` 且 `status=ok`（防止端口被其他服务占用时误判健康）；非健康（不可达/非 200/错误服务/畸形体）统一 degraded，修正此前非 200 误报 critical 与文档语义的不一致；`scripts/cockpit-watchdog.ps1` `Test-CockpitHealth` 同样校验响应体身份；文档：ops-runbook §1 cockpit 行 + §2.1 健康判定说明。 | `scripts/health_check.py`; `scripts/cockpit-watchdog.ps1`; `docs/operations/ops-runbook.md` | `tests/unit/test_ops_tooling.py`（+4 项：身份 ok/错误服务 degraded/非 200 degraded/畸形体 degraded）; `tests/unit/test_preflight_watchdog.py`（+2 项：真实驾驶舱 healthy/伪造服务 would restart） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 896 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，只读响应体/无敏感数据/无新增依赖）；protocol 不涉及 | done | `d856660` |
| ENG-BASE | OPS-6 | 健康检查备份完整性校验（2026-08-04，用户指令：继续生产落地优化）：`scripts/health_check.py` 新增 `--verify-backups`——对最新备份执行 `backup_state.py verify`（完整性哈希校验；子进程 120s 超时 fail-closed；工具缺失/失败/超时 → degraded），成功时 detail 含 `integrity=ok`；校验仅在新鲜度 ok 时执行（可选、有界成本，闭环 OPS-3 的"完整性校验留手动"边界）；文档：ops-runbook §1 backup 行 + 示例。 | `scripts/health_check.py`; `docs/operations/ops-runbook.md` | `tests/unit/test_ops_tooling.py`（+3 项：verify ok/verify 失败 degraded 断言消息/verify 超时 degraded） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 899 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，只读子进程/无敏感数据/无新增依赖）；protocol 不涉及 | done | `c001c0c` |
| ENG-BASE | AVAIL-3 | 看门狗重启预算（2026-08-04，用户指令：继续生产落地优化）：新增 `scripts/restart-budget.ps1`——纯函数 `Test-RestartBudget`（窗口内重启次数 `< MaxRestarts` 才允许），可被看门狗 dot-source，也可独立运行供测试；`scripts/cockpit-watchdog.ps1` 新增 `-MaxRestarts`（默认 5）与 `-RestartWindowSeconds`（默认 3600），重启前判定预算，耗尽时继续轮询但停止重启并打印 manual intervention required（窗口滚动后恢复）；文档：ops-runbook §2.1 重启预算说明。 | `scripts/restart-budget.ps1`（新）; `scripts/cockpit-watchdog.ps1`; `docs/operations/ops-runbook.md` | `tests/unit/test_preflight_watchdog.py`（+5 项：空历史 allowed/预算内 allowed/耗尽 denied/窗口外旧时间戳忽略/非法参数失败关闭） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 904 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，纯计算/无敏感数据/无新增依赖）；protocol 不涉及 | done | `9c32b82` |
| ENG-BASE | CI-2 | 工具链制品构建与哈希回填（2026-08-04，用户指令：push 到 github 后继续）：新增 `scripts/ci-build-toolchain.py`——可复现构建离线工具链 zip（python 完整运行时+锁文件/node/gmssl/control，根为 `.tools/`），输出大小与 SHA-256，fail-closed（缺条目/输出已存在拒绝）；`docs/dependencies/ci-artifact.json` 回填 version=1.0.0/url=Release 模式/sha256=81dd3e7d…（80.08 MB、4934 文件）；真实制品经 `ci-restore-toolchain.ps1 -LocalPath` 恢复成功，恢复出的 python 跑 fmt/lint 均 exit 0（指纹 e225df61/4e9985cf，CI 场景指纹与维护机不同属预期）；文档：ci-artifact-hosting.md 状态/构建命令、known-limitations CI 行更新；剩余激活步骤：所有者创建 `toolchain-1.0.0` Release 上传制品。 | `scripts/ci-build-toolchain.py`（新）; `docs/dependencies/ci-artifact.json`; `docs/operations/ci-artifact-hosting.md`; `docs/operations/known-limitations.md` | `tests/unit/test_ci_restore.py`（+3 项构建夹具：布局/缺条目拒绝/输出已存在拒绝；pending 描述符测试改临时夹具；描述符已回填断言） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 907 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，仅 zip/哈希/无敏感数据/无新增依赖）；protocol 不涉及 | done | `d198002` |
| ENG-BASE | RECORDS-2 | BACKLOG 状态补正（2026-08-04，用户指令：继续生产落地优化）：`loop/BACKLOG.yaml` 19 个已完成项 `ready → done`（恢复早期状态补正惯例，`ready` 仅用于未开始项），消除与 STATE/matrix 的矛盾——release_check 的 backlog 检查由"19 ready item(s) explicitly deferred"变为"all items done"；新增回归测试：BACKLOG 非 done 项必须恰为 STATE `current_item`（三源一致性不变量）。 | `loop/BACKLOG.yaml`; `tests/unit/test_release_check.py` | `tests/unit/test_release_check.py`（+1 项：真实 BACKLOG/STATE 一致性不变量） | `make quality` exit=0 fingerprint=`e3a61c2f23c3031b`；audit fully-sealed；unit 908 / integration 259 / security 97 / e2e 14 全绿；security 内联放行（Critical/High 0，纯记录状态/无代码语义变化）；protocol 不涉及 | done | `4ded745` |
| ENG-BASE | REVIEW-FIX-3 | 整体审查问题修复（2026-08-06，用户指令：进行所有问题的修复与优化）：① H-1 单实例锁——`SingleInstanceLock` 心跳刷新（默认 60s `os.utime`）+ `_recover_stale` 双判定（mtime 过期且记录 pid 不再存活才接管），Windows 存活探测 `OpenProcess`+`GetExitCodeProcess(STILL_ACTIVE)` 且显式声明 64 位 HANDLE restype，心跳线程随 start/stop 启停；② M-1 e2e ResourceWarning——launcher 关闭子进程 stdout/stderr、offline_frontend/integration 的 HTTPError `finally close`，e2e 日志 ResourceWarning=0；③ M-2 质量门禁互斥——`quality_gate.py` 用 `exclusive_lock(GATE_LOCK)`（`loop/.quality-gate.lock`，gitignored）串行化，main 捕获 RuntimeError 输出 exit_code=15，文档注明门禁必须串行；④ L-2 `_serve_static` 仅对 text/* 追加 charset；⑤ L-3 驾驶舱响应头——`_send_bytes` 字典默认头（nosniff/no-store/no-referrer，静态可覆盖 Cache-Control 为 public max-age=300），index/API 不缓存且不泄露 referrer，集成/e2e 断言；⑥ L-4 归档阈值下调（verification keep_recent 60→30、size 1M→500K）+ 策略文档同步 + 测试固定；⑦ secret_scan 对 `loop/` 记录文件豁免 pem/pgp/key_assignment 夹具模式（令牌类仍全路径拦截）+ 单测；锁链同步（quality_gate.py 哈希 / python-script-lock.tsv / toolchain-lock script_inventory+source_sha256 / make.cs ScriptInventorySha256）。 | `src/coevo/cockpit/server.py`; `scripts/quality_gate.py`; `scripts/secret_scan.py`; `scripts/archive_records.py`; `scripts/tool-shims/make.cs`; `docs/dependencies/python-script-lock.tsv`; `docs/dependencies/toolchain-lock.json`; `docs/process/records-archiving-policy.md` | `tests/unit/test_cockpit_http.py`（+4 项锁/心跳/进程探测）; `tests/unit/test_quality_gate_lock.py`（新，3 项）; `tests/unit/test_secret_scan.py`（+3 项 loop 豁免）; `tests/unit/test_records_archive.py`（+2 项策略固定）; `tests/integration/test_cockpit_http_server.py`（+4 项）; `tests/e2e/test_cockpit_launcher.py`（+1 项）; `tests/e2e/test_cockpit_offline_frontend.py`（+2 项） | 主仓库 `make quality` exit=0 fingerprint=`759566939f0be77b`（2026-08-06T14:36:30Z）；全套件独立复核 unit 929 / integration 259 / security 97 / e2e 14 全绿、e2e ResourceWarning=0；audit fully-sealed；security-reviewer PASS（见 DECISIONS 2026-08-06 复核条目；沙箱 junction 与锁定工具目录 reparse-point 检查冲突为 2026-08-02 已知决策项，按先例以主仓库门禁+专家只读审查放行）；protocol 不涉及 | done | `591606f`+`c2b4737` |
| ENG-BASE | OPTIMIZE-1 | 逐文件深度审查与模块文档细化（2026-08-06，用户指令）：① 全仓静态扫描（AST：语法/可变默认参数/裸 except/eval/TODO）+ 逐文件代码走查（数据结构/算法/架构）——无语法错误、无可变默认参数、无裸 except；② 清理 14 处死导入（`report/builder.py`、`audit_governance/models.py`、`config.py`、`knowledge_base/store.py`、`talent/*`、`task_decomposition/*`、`task_flow/*`、`workspace/init_service.py`、`benchmarks/__init__.py`）；③ 修复 `health_check.check_audit` 语义矛盾——改用 `verify --allow-tail`，审计尾部未密封 = degraded（原实现因无 status 判为 critical，与文档不符），补 4 项单测（fully-sealed ok / 未密封尾部 degraded / 真实失败 critical / 超时 critical）；④ `docs/modules/` 22 份模块 README 按统一模板细化（定位/职责边界/文件与关键类型/数据流/安全不变量/测试覆盖/依赖与下游）。 | `scripts/health_check.py`; `src/coevo` 14 个文件（死导入清理）; `docs/modules/`（22 份模块 README） | `tests/unit/test_ops_tooling.py`（+4 项 check_audit）; 全量回归 unit / integration 259 / security 97 / e2e 14 全绿 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目，指纹随本轮命令集）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：无阻断项；观察项：pid 重用极端场景下锁接管可用性边界、loop/ PEM 豁免范围，令牌类仍全路径拦截）；protocol 不涉及 | done | `168d768` |
| ENG-BASE | OPTIMIZE-2 | 第二轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-1）：① 生产代码 `assert` 改显式失败关闭（`decision_brief/models.py` `_make_version`/`_validate_template_ref`、`model/__init__.py` `select_provider`、`protocol/agent_package.py` Fixed Header 布局检查——`python -O` 下不再失效）；② 再清理 19 处死导入——src（`audit_governance/facade.py`、`cockpit/server.py`、`protocol/import_service.py`、`protocol/package_builder.py`、`report/models.py`、`task_decomposition/baseline.py`、`workspace/init_service.py`）、scripts（`review_sandbox.py`、`secret_scan.py`、`audit_key_health.py`、`ci-build-toolchain.py` 的 sys）、examples（`service-api` 适配器/框架/服务器/测试、`tool-dev-project/run_example.py` 的 stdlib 与未用域导入，共 10 文件）；③ 模块 README 进一步细化——安全关键模块补充"配置与错误语义"章节（环境变量/HTTP 语义/异常类型），索引说明模板；④ 全仓文档与代码一致性校验（文件清单无遗漏/无悬空）。 | `src/coevo` 10 个文件; `scripts` 4 个文件; `examples` 10 个文件; `docs/modules/`（19 份更新） | `tests/unit/test_decision_brief.py`; `tests/unit/test_model_provider.py`; `tests/unit/test_audit_governance.py`; `tests/unit/test_cockpit_http.py`; `tests/integration/test_cockpit_http_server.py`; `examples/service-api/tests/test_framework.py`; `examples/service-api/tests/test_e2e_smoke.py` | 全量回归 unit / integration 259 / security 97 / e2e 14 + examples 41 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：assert 改显式抛出不改变失败关闭语义，死导入清理零行为变更，锁链未动）；protocol 不涉及 | done | `4631266` |
| ENG-BASE | OPTIMIZE-3 | 第三轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-2）：① 补读剩余模块（cockpit facade/models/state_store/static、progress_capture service、supervision service、merge models、crypto contract/key_handle、model prompts/deepseek、scripts backup_state/audit_seal/run_cockpit/release_check）——未发现新的算法或结构缺陷（复杂度热点集中在刻意穷举的校验函数与演示脚本）；② 修复 `supervision/service.py` 冗余双 `_parse_utc`（一次校验一次取值 + 空 try/except）为单次解析；③ `.opencode/plugins/loop-guard.ts` 护栏与 `.codex/hooks/loop-guard.mjs` 对齐——补 `Invoke-RestMethod` 与 `(pnpm|yarn) (install|add)` 拦截（原 TS 护栏弱于 MJS）；④ 模块 README 补充"性能与复杂度"章节（10 个模块：依赖图/映射/评分/收据/watcher/审计流/编排/简报/工作区/协议）。 | `src/coevo/supervision/service.py`; `.opencode/plugins/loop-guard.ts`; `docs/modules/`（10 份更新） | `tests/unit/test_supervision_meeting.py`; `tests/security/test_loop_guard_static.py`; `tests/security/test_local_toolchain_security.py` | 全量回归 unit / integration 259 / security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：护栏对齐为收紧拦截、冗余解析清理零行为变更）；protocol 不涉及 | done | `b6de86a` |
| ENG-BASE | OPTIMIZE-4 | 第四轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-3）：① 补查安全关键 PowerShell/C# 助手（invoke-gmssl-crypto/audit_signature/windows-native-security/gmssl helpers）——句柄锁定、reparse-point 拒绝、非导出密钥强制、环境净化均正确，无新问题；② opencode 工具链一致性（loop_state.ts 包装、path-policy.mjs 保护集）与测试质量抽查（无空测试/弱断言集中）通过；③ examples 全量联合验证（run_all.py：tool-dev-project + service-api 41 项）通过；④ 模块文档引用核验无过时符号；⑤ 清掉最后 2 处锁定脚本死导入（`check_loop_stop.py`/`traceability_check.py` 的 sys）并同步锁链（TSV/toolchain-lock/make.cs，dev-environment 24 项回归通过）；⑥ `deepseek.py` 重复声明 `HttpPost` 改为从 `openai_compatible` 导入；⑦ 文档索引新增模块依赖关系 mermaid 图。 | `scripts/check_loop_stop.py`; `scripts/traceability_check.py`; `scripts/tool-shims/make.cs`; `docs/dependencies/python-script-lock.tsv`; `docs/dependencies/toolchain-lock.json`; `src/coevo/model/deepseek.py`; `docs/modules/README.md` | `tests/integration/test_dev_environment_entry.py`; `tests/unit/test_dev_environment_tools.py`; `tests/security/test_local_toolchain_security.py`; `tests/unit/test_model_provider.py` | 全量回归 unit / integration 259 / security 97 / e2e 14 全绿；examples run_all 通过；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：锁链同步一致、死导入清理零行为变更、护栏未放宽）；protocol 不涉及 | done | `edeafee` |
| ENG-BASE | OPTIMIZE-5 | 第五轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-4）：① 测试套件质量扫描——发现 5 个无断言测试，其中 3 个为真弱断言（`test_payload_block_header_is_valid` 仅"不抛错"、2 个 base-revision 测试），已补回读数据/块大小一致性断言与"未回滚"断言，另 2 个断言在辅助函数内无需改；② 协议 `payload_length` 一致性修复（真实缺陷）——`build_encrypted_package` 会归一化 envelope 而 `build_unsigned_package` 不归一化、`parse_package_bytes` 不校验 envelope 与 Fixed Header 一致性（`parse_package_header` 却严格校验，两条解析面行为不一致）；修复为 builder 归一化 + parser 强制一致，184 项协议相关测试全绿；③ CI 工作流（.github/workflows/quality.yml）复核：工具链恢复、分目标门禁、证据上传、签名门禁维护机执行说明均与门禁模型一致；④ 文档补充测试运行指引与协议 payload_length 语义说明。 | `src/coevo/protocol/package_builder.py`; `tests/integration/test_agent_package_aead.py`; `tests/integration/test_agent_package_atomic_import.py`; `docs/modules/protocol.md`; `docs/modules/README.md` | `tests/integration/test_agent_package_aead.py`; `tests/integration/test_agent_package_atomic_import.py`; `tests/integration/package_header_test.py`; `tests/unit/test_report_builder.py`; `tests/unit/test_protocol_sign_blocked.py` | 全量回归 unit / integration 259 / security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：payload_length 一致性修复为收紧解析面、测试补断言零行为变更）；protocol 涉及但仅收紧一致性（非 wire 变更） | done | `0185ed3` |
| ENG-BASE | OPTIMIZE-6 | 第六轮逐文件深度审查与文档细化（2026-08-07，用户指令"继续"，延续 OPTIMIZE-5）：① 测试断言语义全仓扫描（assertTrue(x==y)/assertTrue(isinstance)/assertEqual(x,False)/assertTrue(not) 等模式）——套件整体健康，仅 2 处可精确化：`test_model_provider.py` `assertTrue(provider.name == "local_openai")` 改 `assertEqual`、`private_key_windows_store_test.py` `assertEqual(reference["revoked"], False)` 改 `assertFalse`（失败信息更清晰、语义更准）；其余 assertTrue(all/any(...)) 为集合谓词断言属合理用法；② 文档索引补充"关键常量与闭集枚举索引"总览表（18 个模块的闭集枚举/关键常量，含 StandardStage/AgentCapability/PACKAGE_TYPES/MergeDecision/InterceptionReason/ProviderScope 等）。 | `tests/unit/test_model_provider.py`; `tests/integration/private_key_windows_store_test.py`; `docs/modules/README.md` | `tests/unit/test_model_provider.py`; `tests/integration/private_key_windows_store_test.py` | 全量回归 unit / integration 259 / security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：断言语义精确化零行为变更、文档无安全影响）；protocol 不涉及 | done | `f248e23` |
| ENG-BASE | OPTIMIZE-7 | 第七轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-6）：① docstring/类型注解覆盖审计——228 处"缺失"经核对均为仓库既定排除策略（属性 getter、协议单行桩、异常类、数据类），不churn；② 变异探针式边界核查发现并修复真实缺陷：`replay_detector.check_replay` 允许**同序号但不同内容**的包通过（`<` 应为 `<=`），与协议 §13"递增序号"矛盾，同序号包无法区分新旧、削弱重放检测——收紧为严格递增并补等序号重放测试；③ `sanitize_id` 补边界测试（恰好 64 字符合法、65 非法，防最大长度+1 类变异漏网）；④ 文档索引补英文总览（Overview）。 | `src/coevo/protocol/replay_detector.py`; `tests/integration/test_agent_package_aead.py`; `tests/unit/test_workspace_init.py`; `docs/modules/protocol.md`; `docs/modules/README.md` | `tests/integration/test_agent_package_aead.py`; `tests/integration/test_agent_package_atomic_import.py`; `tests/unit/test_workspace_init.py` | 全量回归 unit / integration 260（+1 等序号重放）/ security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：同序号收紧为拒绝重排/重放异常，属失败关闭方向，未放宽任何检查）；protocol 涉及重放语义但非 wire 变更 | done | `e78c27d` |
| ENG-BASE | OPTIMIZE-8 | 第八轮逐文件深度审查与文档细化（2026-08-07，用户指令"继续"，延续 OPTIMIZE-7）：① 重放严格递增收紧的**跨作用域回归测试**——同序号在不同项目/不同接收人作用域下仍应 ACCEPT（序号空间按接收人隔离），防过度收紧；② merge 决策者白名单**空集合边界测试**——`authorized_recipient_certs=frozenset()` 必须拒绝一切（失败关闭，接收人不在空集内）；③ 文档新增英文模块总览 [README.en.md]（21 模块各一段英文摘要），索引加链接。 | `tests/integration/test_agent_package_aead.py`; `tests/unit/test_merge_engine_v3.py`; `docs/modules/README.en.md`; `docs/modules/README.md` | `tests/integration/test_agent_package_aead.py`; `tests/unit/test_merge_engine_v3.py` | 全量回归 unit / integration 261（+1 跨作用域）/ security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：空白名单拒绝与跨作用域接受均为既有语义的边界回归，零行为变更）；protocol 不涉及 | done | `f036a5c` |
| ENG-BASE | OPTIMIZE-9 | 第九轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-8）：① 安全审计拦截判定变异探针——五类原因（CORRUPTED/TAMPERED/EXPIRED/DUPLICATE/RECIPIENT_MISMATCH）均有测试且"损坏短路篡改"优先级已覆盖；补 2 项边界：过期恰好等于 now 不判过期（`now > expiration_ts` 严格比较）、损坏+过期双因并存时按位置顺序全部列出（损坏只短路篡改，不吞其他原因）；② 5 个安全关键模块英文全量文档（protocol/crypto/identity/merge/audit_governance 的 .en.md，含范围/边界/文件/数据流/安全不变量/测试/依赖/错误与性能），README.en.md 加链接。 | `tests/unit/test_audit_governance.py`; `docs/modules/protocol.en.md`; `docs/modules/crypto.en.md`; `docs/modules/identity.en.md`; `docs/modules/merge.en.md`; `docs/modules/audit_governance.en.md`; `docs/modules/README.en.md` | `tests/unit/test_audit_governance.py` | 全量回归 unit / integration 261 / security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：边界测试为既有语义回归，英文文档无安全影响）；protocol 不涉及 | done | `113437b` |
| ENG-BASE | OPTIMIZE-10 | 第十轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-9）：① 依赖图环检测边界探针——自环拒绝、双节点环、三节点环（a→b→c→a）、拓扑确定性、未知任务 ID 拒绝均已有测试，覆盖完备无需补；② 补完剩余 16 个模块英文全量文档（app/benchmarks/cockpit/decision_brief/knowledge_base/model/orchestrator/progress_capture/report/risk/root_modules/supervision/talent/task_decomposition/task_flow/workspace 的 .en.md），至此 21/21 模块中英文文档全覆盖，README.en.md 链接齐全。 | `docs/modules/`（16 份 .en.md 新增）; `docs/modules/README.en.md` | `tests/unit/test_task_decomposition.py`; `tests/unit/test_optimizations.py` | 纯文档无测试变更；全量门禁回归见 VERIFICATION；主仓库最终 `make quality` exit=0；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：纯文档、零代码行为变更） | done | `ac36318` |
| ENG-BASE | OPTIMIZE-11 | 第十一轮逐文件深度审查与优化（2026-08-07，用户指令"继续"，延续 OPTIMIZE-10）：① 锁定脚本 `check_loop_stop.py` 测试覆盖核查——此前零测试；发现真实健壮性缺陷：STATE.json 畸形/非对象时 `json.loads` 直接崩溃而非按失败关闭返回阻断码；修复为捕获 JSONDecodeError/UnicodeDecodeError/OSError 与非对象载荷并返回 20；② 新增 6 项退出码测试（缺文件 20 / mvp-complete 0 / done/ready 10 / 三种阻断态 20 / 畸形 JSON 20 / 非对象 20）；③ 补 6 处缺失返回类型注解（audit_anchor.exclusive_lock/locked、merge engine receipt_builder、merge models _to_jsonable、real_chain_store _guard、report models _replace）；④ 锁链同步（check_loop_stop 哈希/TSV/toolchain-lock/make.cs，dev-environment 回归通过）。 | `scripts/check_loop_stop.py`; `tests/unit/test_check_loop_stop.py`（新）; `src/coevo/identity/audit_anchor.py`; `src/coevo/merge/engine.py`; `src/coevo/merge/models.py`; `src/coevo/orchestrator/real_chain_store.py`; `src/coevo/report/models.py`; `scripts/tool-shims/make.cs`; `docs/dependencies/python-script-lock.tsv`; `docs/dependencies/toolchain-lock.json` | `tests/unit/test_check_loop_stop.py`; `tests/integration/test_dev_environment_entry.py`; `tests/security/test_local_toolchain_security.py` | 全量回归 unit（+6 新测试）/ integration 261 / security 97 / e2e 14 全绿；e2e ResourceWarning=0 | 主仓库最终 `make quality` exit=0（见 VERIFICATION 最新条目）；audit fully-sealed；security-reviewer PASS（静态 STRIDE 复核：畸形状态失败关闭为收紧、类型注解零行为变更、锁链一致）；protocol 不涉及 | done | `bfef13c` |
| ENG-BASE | OPTIMIZE-12 | 第十二轮逐文件深度审查与文档细化（2026-08-07，用户指令，延续 OPTIMIZE-11；用户明确"不用做全量质量门"）：① 审查 `examples/tool-dev-project/run_example.py` 巨型 `main`（约 2900 行，CC≈77）——结论为线性叙事演示，闭包共享大量局部状态，机械拆分行为漂移风险高、收益有限，保持现状并在示例 README 留痕维护说明；② 为锁定维护工具 `scripts/split_packages.py` 补 5 项测试（此前零测试）：行偏移、装饰器保留切片、node_names、used_names+导入剪枝、split_package 拆分重写（patch ROOT 到临时目录，验证生成 models.py 与 __init__ 重导出及 __all__ 保留）。 | `tests/unit/test_split_packages.py`（新）; `examples/tool-dev-project/README.md` | `tests/unit/test_split_packages.py` | 定向验证 5 项测试全绿（`python -m unittest tests.unit.test_split_packages`）；**按用户指示未跑全量质量门**（待下次全量门禁回归） | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `20ed0d3` |
| ENG-BASE | OPTIMIZE-13 | 第十三轮逐文件深度审查（2026-08-07，用户指令，延续 OPTIMIZE-12；用户明确"不用做全量质量门"）：剩余纯函数边界探针，补 3 项测试：① 任务流 `apply_mapping` 自定义规则——优先级数值小者胜（10 胜 20），同优先级按 rule_id 字典序决胜（rule.a 胜 rule.b）；② 督办升级时间边界——`due_at == now` 恰好到期时 `delta >= 0` 分支生效（SEVERE_COORDINATION_NEEDED → EMERGENCY、LONG_SILENCE → WATCH）；③ 知识包密级——AC-5 取全部条目最高密级，INTERNAL 基线 + RESTRICTED 简报 → RESTRICTED。 | `tests/unit/test_task_flow_models.py`; `tests/unit/test_supervision_meeting.py`; `tests/unit/test_knowledge_base.py` | `tests/unit/test_task_flow_models.py`; `tests/unit/test_supervision_meeting.py`; `tests/unit/test_knowledge_base.py` | 定向验证 59 项全绿（含 3 项新增）；**按用户指示未跑全量质量门**（待下次全量门禁回归） | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `ff234fd` |
| ENG-BASE | OPTIMIZE-14 | 第十四轮逐文件深度审查（2026-08-07，用户指令"继续"，延续 OPTIMIZE-13；不做全量质量门）：覆盖核查 `run_validation.py`（锁定报告脚本，此前零测试；依赖 PyYAML，须在锁链 python 下运行——已确认 .tools/python 捆绑 yaml 6.0.3，属受控环境设计）；补 4 项测试：JSONC 行/块注释剥离且保留字符串内 `//`、指标采集（opencode.jsonc permission/skill/task 解析、BACKLOG 状态计数、tool-audit 行数，patch ROOT 到临时目录）、文本渲染包含关键字段。 | `tests/unit/test_run_validation.py`（新） | `tests/unit/test_run_validation.py` | 定向验证 4 项全绿（`E:\Workspace\Coevo\.tools\python\3.14.3\python.exe -m unittest tests.unit.test_run_validation`）；**按用户指示未跑全量质量门** | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `567981a` |
| ENG-BASE | OPTIMIZE-15 | 第十五轮逐文件深度审查（2026-08-07，用户指令"继续"，延续 OPTIMIZE-14；不做全量质量门）：覆盖核查 `control_main.py`（锁定 control.pyz 分派入口，此前仅源码内容断言无行为测试）；补 4 项行为测试：缺参退出（usage）、未知模块退出（usage）、runpy.run_module 分派（模块名/run_name=__main__/alter_sys=True、argv 重写与消费）、MODULES 清单为 6 个预期模块。 | `tests/unit/test_control_main.py`（新） | `tests/unit/test_control_main.py` | 定向验证 4 项全绿；**按用户指示未跑全量质量门** | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `a18a354` |
| ENG-BASE | OPTIMIZE-16 | 第十六轮逐文件深度审查（2026-08-07，用户指令"继续"，延续 OPTIMIZE-15；不做全量质量门）：文档治理测试核查——现有 test_production_docs 仅覆盖环境变量文档一致性；新增 `tests/unit/test_module_docs.py` 4 项强制守卫：① 每个 src/coevo 包必须有 docs/modules/<pkg>.md；② 每份文档的文件清单须列出包内全部非 __init__ .py 文件（防缺文件）；③ 根级模块（config/version/logging/records）须在 root_modules.md 覆盖；④ README 索引须列出全部包（防新增模块缺文档）。 | `tests/unit/test_module_docs.py`（新） | `tests/unit/test_module_docs.py` | 定向验证 4 项全绿；**按用户指示未跑全量质量门** | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `ac66188` |
| ENG-BASE | OPTIMIZE-17 | 第十七轮逐文件深度审查（2026-08-07，用户指令"继续"，延续 OPTIMIZE-16；不做全量质量门）：破坏性清理脚本核查——`force-remove-loop-runtime*.ps1` 永久删除 `loop/runtime`，此前零测试；审查确认主变体有硬编码期望路径精确匹配 + reparse-point 拒绝 + robocopy /XJ 防穿越 + DELETE 确认 + 删后验证，win32 变体有 reparse 处理 + 确认 + 删后验证；新增 `tests/security/test_force_remove_safety.py` 2 项静态守卫：两脚本的删除目标必须等于仓库 `loop/runtime` 字面量，且保留确认/防穿越/防 reparse 护栏（防未来编辑静默改路径或删护栏）。 | `tests/security/test_force_remove_safety.py`（新） | `tests/security/test_force_remove_safety.py` | 定向验证 2 项全绿；**按用户指示未跑全量质量门** | 全量质量门按用户指示豁免（2026-08-07）；定向测试通过；DECISIONS 已记录豁免原因 | done | `a15a392` |
| GO-MIGRATE | GO-ENV-1 | Go 工具链锁定与环境就绪（离线源 `D:\Go` go1.18.8，toolchain-lock 登记 + 许可证文件）+ 首个迁移切片：`task_flow` 纯模型与阶段映射 Go 移植（SourceKind / StandardStage / Traced / SourceMapping / ProcessFlow / WithOverrides / ApplyMapping / 27 条默认映射规则，行为与 Python 对齐）+ `go test ./...` 接入质量门禁（`GOPROXY=off` 强制离线、stdlib-only） | `docs/dependencies/toolchain-lock.json`; `docs/dependencies/licenses/go-BSD-3-Clause.txt`; `go/go.mod`; `go/taskflow/doc.go`; `go/taskflow/models.go`; `go/taskflow/mapping.go`; `scripts/quality_gate.py`; `scripts/tool-shims/make.cs`; `docs/dependencies/python-script-lock.tsv` | `go/taskflow/models_test.go`; `go/taskflow/mapping_test.go` | go test 13/13 全绿（gofmt/vet 干净）；`make quality` exit=0（见 VERIFICATION 最新条目，含 Go 步骤）；audit fully-sealed | done | `780ab6d` + records |
| US-16 | AC-1 | 框架层 manifest-checker（CTAF §5.3 / M1a）：Agent Manifest 强制校验（能力闭集 T2 / 人工确认缺省 true T3 / crypto_scope 闭集 T4 / 审计脱敏子集 T5 / spec_hash 排除自指字段 F5 / policy_ref 三段绑定且公钥取自证书链 F8 / policy_version 绑定 F7 / 失败不注册 / 纯函数离线零三方依赖 L15）+ `.agent` v1.0 wire 字节级回归 T6 | `src/coevo/framework/manifest_checker.py`; `src/coevo/framework/__init__.py`; `docs/modules/framework.md` | `tests/unit/test_framework_manifest_checker.py`; `tests/unit/test_agent_wire_regression.py` | 定向 31/31 全绿；`make quality` exit=0 fingerprint=`34d637f035600903`（2026-08-07T11:59:49Z，audit fully-sealed）；mvp-verifier PASS（10/10 AC）；security-reviewer PASS（Critical/High 0；M1 已就地修复 `9fcc906`） | done | `5536c92` + `9fcc906` |
| US-16 | AC-2 | 框架层 Policy 抽象与 validate_plan（CTAF §6.5 / M2）：Policy 字段与 policy_version 必填（F7）、4 默认 Profile（max_recover_attempts ≤ 3 对齐 L16）、EMERGENCY fail-fast（1 次重试 / 60s / 事后 30 分钟确认 / 本地告警，F1+F9）、L18 白名单（tool_args 数值按 schema 允许，F6）、validate_plan 五项不变量 + L18 + L19（A9/F4）、L19 状态机（ESCALATED→ACTIVE 必须经 HELD，RETIRED 直退）、纯函数离线零三方依赖（L15）+ L17 文档守卫 | `src/coevo/framework/policy.py`; `src/coevo/framework/plan.py`; `src/coevo/framework/lifecycle.py`; `src/coevo/framework/validation.py`; `src/coevo/framework/__init__.py`; `docs/modules/framework.md` | `tests/unit/test_framework_policy.py`; `tests/unit/test_framework_plan_l18.py`; `tests/unit/test_framework_lifecycle.py`; `tests/unit/test_framework_validate_plan.py` | 定向 74/74 全绿；`make quality` exit=0 fingerprint=`34d637f035600903`（2026-08-07T16:32:24Z，audit fully-sealed）；mvp-verifier PASS（10/10 AC，沙箱门禁失败均为环境性）；security-reviewer PASS（Critical/High 0；Low 3 项已就地修复 `b23d85b`） | done | `7a3ed8b` + `b23d85b` |
| US-16 | AC-3 | 框架层能力闭集收敛（CTAF §5.2 / M1b）：能力注册表 19 项（12 MVP 映射 AgentCapability / CRYPTO_PROXY 限 approved-product / 6 框架抽象 PLANNER..HUMAN_GATE）、双名解析（枚举值 / CTAF 规范名 / 成员名同条目，闭集外与大小写变体 fail-closed）、manifest-checker 校验切换（MVP 未映射拒绝、CRYPTO_PROXY approved scope、框架抽象可用）、双向一致性守卫（无孤儿 / 无未映射 MVP）、capability-closedset.md 文档 + L15 stdlib / L17 文档守卫 | `src/coevo/framework/capability.py`; `src/coevo/framework/manifest_checker.py`; `src/coevo/framework/validation.py`; `src/coevo/framework/__init__.py`; `src/coevo/orchestrator/models.py`; `docs/framework/capability-closedset.md`; `docs/modules/framework.md` | `tests/unit/test_framework_capability.py`; `tests/unit/test_framework_manifest_checker.py`; `tests/unit/test_framework_validate_plan.py`; `tests/unit/test_framework_plan_l18.py`; `tests/unit/test_framework_lifecycle.py` | 定向 75/75 全绿 + 相邻回归 38/38（orchestrator / wire / real-facade-chain）；fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（2026-08-07T16:42Z，audit fully-sealed）；按用户指示本轮豁免全量 quality（DECISIONS 留痕）；security-reviewer 契约沙箱内执行 PASS：Critical/High 0，Low 2 观察项 | done | `b42d00c` + 本轮 records |
| US-16 | AC-4 | 框架层 Memory 抽象（CTAF §6.2 / M3）：MemoryRecord 统一模型（EPISODIC/SEMANTIC + 规范化指纹）、Episodic 审计投影必填（固定五键）、Semantic 审批（映射 knowledge_base ReviewDecisionKind.APPROVE，未审批拒绝）、L12 敏感字段经注入 Redactor 转 `REDACTED:<sha256>` 摘要（明文不得到达 store）、注入 store/审批/Redactor 异常 fail-closed、memory-interface.md + L15 stdlib / L17 文档守卫 | `src/coevo/framework/memory.py`; `src/coevo/framework/__init__.py`; `docs/framework/memory-interface.md`; `docs/modules/framework.md` | `tests/unit/test_framework_memory.py` | 定向 104/104 全绿；lint exit=0 fingerprint=`252ad24e526f6728`（主仓库增量门禁，audit fully-sealed）；按用户指示本轮豁免全量 quality（DECISIONS 留痕）；security-reviewer PASS：Critical/High 0，Low 3 观察项（1 项已就地修复 `727e739`） | done | `c988cd9` + `727e739` |
| US-16 | AC-5 | 框架层 Tool 抽象与 MCP schema 路径 A（CTAF §6.3 / §7.2 / M4）：Tool 统一模型（tool_id safe-id / tool_version P2 必填 semver / side_effects 闭集 / requires_consent / timeout_sec 严格正整数 / size_in_bytes_max 严格非负整数 / crypto_scope ProviderScope 闭集 / audit_required / input/output schema）、工具注册表（校验注册分离、重复拒绝、容量 128）、Tool<->MCP 描述双向转换（name/description/inputSchema/outputSchema + x-coevo 扩展块，子集往返字节级一致，缺失扩展块/未知顶层与扩展键显式拒绝）、JSON Schema 子集校验（type/properties/required/items/enum/description 白名单，object 必带 properties、required 为 properties 子集、array 必带 items、enum 非空≤64、未知关键字/深度>16/大小>16KiB 拒绝，fail-closed）、纯函数离线 stdlib（零 MCP SDK，L15）+ L17 文档守卫（tool-registry.md） | `src/coevo/framework/tools.py`; `src/coevo/framework/__init__.py`; `docs/framework/tool-registry.md`; `docs/modules/framework.md` | `tests/unit/test_framework_tools.py`; `tests/unit/test_module_docs.py` | 定向 111/111 全绿；fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（增量门禁，audit fully-sealed）；按用户指示本轮豁免全量 quality（DECISIONS 留痕）；security-reviewer 契约沙箱内执行 PASS：Critical/High 0，Low 1 已就地修复 `65dfb1e` | done | `d2f4046` + `65dfb1e` + records |
| US-16 | AC-6 | 框架层 A2A wire 0.1 与 policy_ref 三段绑定（CTAF §7.3 / M5）：A2A 消息模型全字段校验 fail-closed（AC-6.1，含 signature 长度上限 1024 hex）、policy_ref 三段绑定（spec_hash 排除自指字段 / 证书链 DER 指纹 / SM2 验签公钥取自证书链）按 §7.3.3 五步时序验证且注入异常与畸形 manifest 深度均 fail-closed（AC-6.2）、A2A<->`.agent` 字段映射往返一致且 `.agent` v1.0 wire 字节不变（T6 守护，AC-6.3）、业务载荷 >64KiB 必须 RESULT_SUBMISSION+payload_ref 拆分（AC-6.4）、纯函数离线 stdlib + 审计投影 + L17 文档守卫（AC-6.5，a2a-protocol.md） | `src/coevo/framework/a2a.py`; `src/coevo/framework/manifest_checker.py`; `src/coevo/framework/__init__.py`; `docs/framework/a2a-protocol.md`; `docs/modules/framework.md` | `tests/unit/test_framework_a2a.py`; `tests/unit/test_agent_wire_regression.py`; `tests/unit/test_module_docs.py` | 定向 126/126 全绿（AC-6 13 项 + 框架族/wire/文档守卫相邻回归）；fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（增量门禁，audit fully-sealed）；按用户指示本轮豁免全量 quality（DECISIONS 留痕）；security-reviewer + protocol-reviewer 契约沙箱内执行 PASS：Critical/High 0，Low 2 已就地修复 `6ed67b0` | done | `0c0f9c9` + `28c26ac` + `6ed67b0` + records |
| US-16 | AC-7 | 框架层 Plan 规范化序列化（Plan-LSP，CTAF §14.2 / M6）：Plan<->规范 JSON 双向序列化（plan_to_json / json_to_plan / parse_plan_json_bytes，往返字节级一致、重复键/未知字段/BOM/非标准常量/深层嵌套/超限全部 fail-closed）、与 plan_fingerprint 同一规范化规则（防指纹分叉）、validate_plan_json 序列化入口（五项不变量 + L18 + L19，失败返回 REJECTED）、大小/节点/边/tool_args 上限、纯函数离线 stdlib + L17（plan-lsp.md） | `src/coevo/framework/plan.py`; `src/coevo/framework/validation.py`; `src/coevo/framework/__init__.py`; `docs/framework/plan-lsp.md`; `docs/modules/framework.md` | `tests/unit/test_framework_plan_lsp.py`; `tests/unit/test_framework_validate_plan.py`; `tests/unit/test_framework_plan_l18.py`; `tests/unit/test_module_docs.py` | 定向 137/137 全绿；**make quality exit=0 fingerprint=`34d637f035600903`（2026-08-07T18:08:51Z / 18:20:14Z，audit fully-sealed）**；security-reviewer PASS（Critical/High/Medium 0，Low 2 已修复 `edd7cce`） | done | `6cdbf1a` + `edd7cce` + records |
| US-16 | AC-8 | 框架层 Hybrid Orchestrator（CTAF §6.6 / §8 / M7）：OrchestrationEngine 契约 + validate_plan 为 dispatch 前置必调（未校验/校验失败不得 dispatch）、StateMachine 静态链（注入 provider）构建 Plan 并执行（失败/异常 fail-closed 转 ESCALATED + audit RECOVER）、DynamicLLM 提议经五项不变量 + L18 + L19 通过才执行（提议无效/异常回退 StateMachine）、Hybrid 仅非 HOLD 节点可由 LLM 覆盖（链含 HOLD 或提议含 HOLD → 回退链 Plan，HELD 人工门强制、执行器不调用）、L19 八态衔接（ESCALATED→ACTIVE 必须经 HELD）+ 审计投影 + 注入执行器/LLM/链（纯函数离线 stdlib）+ L17（hybrid-orchestrator.md） | `src/coevo/framework/orchestrator.py`; `src/coevo/framework/__init__.py`; `docs/framework/hybrid-orchestrator.md`; `docs/modules/framework.md` | `tests/unit/test_framework_orchestrator.py`; `tests/unit/test_module_docs.py` | 定向 61/61 全绿；fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（增量门禁，audit fully-sealed）；按用户指示本轮豁免全量 quality；security-review 沙箱 PASS（Critical/High 0，Medium 1 已修复 `0c9a352`，Low/Info 3 观察项） | done | `399b20c` + `0c9a352` + records |
| US-16 | AC-9 | 框架层 K8s CRD 纸面清单生成器（CTAF §14.2 / §16.4 / M9）：从能力/Tool/Policy/Plan 声明生成确定性纸面清单（规范 JSON + YAML 安全渲染子集，仅纸面不承诺 reconcile loop、与 K8s 解耦、零 IO）、listing_fingerprint SHA-256 可哈希、opt-in 沙箱纯函数（零 IO 副作用）、清单结构校验 fail-closed（字段白名单 / 重复键 / 未知字段 / BOM / 64KiB 超限 / 64 层深度）、审计投影固定键摘要（不含 spec 明细）、stdlib-only + L17 文档守卫（k8s-crd-listing.md） | `src/coevo/framework/k8s_listing.py`; `src/coevo/framework/__init__.py`; `docs/framework/k8s-crd-listing.md`; `docs/modules/framework.md` | `tests/unit/test_framework_k8s_listing.py`（9 项：AC-9.1..9.5 + 深度上限回归）; `tests/unit/test_module_docs.py` | 增量门禁 fmt `fe39766e2048d2bc` + lint `252ad24e526f6728` exit 0；定向 9/9 + 4/4 全绿；沙箱双签 PASS（Critical/High 0；Low 2：深度递归已修复于 51d4faa、嵌套项未知字段留观察）；audit fully-sealed；全量 quality 按用户指示豁免 | done | `76d15f8` + `ce352f6` + `ac95e57` + `51d4faa` |
| ENG-BASE | FRAMEWORK-GAPS-1 | 框架审查观察项收口（2026-08-08，用户指令"继续开发，先不要全量门禁"）：semver/ISO-8601 格式校验（manifest semantic_version / a2a created_at / memory occurred_at，L7）、Policy 超时上界 dispatch≤600s / plan_total≤7200s / consent≤7200s（Info4）、Orchestrator 链 provider 异常收敛为 OrchestrationError + 审计投影补 validated_at（Low）、K8s 清单 spec 项内按 capability/tool/policy/plan 白名单校验未知键（Low）、文档化取舍（trusted_anchor 委托 L2 / 审计脱敏接线 L4 / 宽捕获 Info5）；纯函数 stdlib + L17 | `src/coevo/framework/manifest_checker.py`; `src/coevo/framework/a2a.py`; `src/coevo/framework/memory.py`; `src/coevo/framework/policy.py`; `src/coevo/framework/orchestrator.py`; `src/coevo/framework/k8s_listing.py` | `tests/unit/test_framework_gaps.py`（302 行，收口项逐条负例 + 相邻框架回归 164 项） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`；定向 164/164 全绿；沙箱 fgaps1-verify（pin=`04b8a5c`）fmt/lint/164 全绿 + fgaps1-sec STRIDE PASS（Critical/High/Medium 0，Low/Info 3 已登记 DECISIONS），check 均 violations=[]；audit fully-sealed；全量 quality 按用户指示豁免 | done | `04b8a5c` + 本轮 records |
| ENG-BASE | FRAMEWORK-GAPS-2 | GAPS-1 新观察项收口（2026-08-08，用户指令"继续开发，先不要全量门禁"）：Policy TimeoutProfile/RetryProfile/ConsentProfile 严格整数类型（bool/str/float/int 子类一律 PolicyValidationError，`type(...) is int`）、semver 严格规则（禁前导对零）、ISO-8601 日历范围校验（datetime.strptime：a2a created_at / memory occurred_at / validate_plan validated_at / transition validated_at）、validated_at 入审计投影前统一 L7 ISO 校验；纯函数 stdlib + L17 | `src/coevo/framework/policy.py`; `src/coevo/framework/manifest_checker.py`; `src/coevo/framework/a2a.py`; `src/coevo/framework/memory.py`; `src/coevo/framework/validation.py`; `src/coevo/framework/orchestrator.py` | `tests/unit/test_framework_gaps2.py`（5 项正/负例 + 相邻框架回归 169 项） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`；定向 169/169 全绿；沙箱 fgaps2-verify（pin=`e29e290`）fmt/lint/169 全绿 + fgaps2-sec STRIDE PASS（Critical/High/Medium 0，Low 1 已登记 DECISIONS），check 均 violations=[]；audit fully-sealed；全量 quality 按用户指示豁免 | done | `e29e290` + 本轮 records |
| ENG-BASE | FRAMEWORK-GAPS-3 | GAPS-2 新观察项收口（2026-08-08，用户指令"继续开发"）：semver 正则 $ 改为 \Z 拒绝尾部换行（1.0.0\n 不再被接受），ISO 路径已由 strptime 兜住；测试 2 项（test_framework_gaps3.py） | `src/coevo/framework/manifest_checker.py` | `tests/unit/test_framework_gaps3.py` | 定向 171/171 全绿；fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（增量门禁，audit fully-sealed）；按用户指示本轮豁免全量 quality（DECISIONS 留痕）；security-reviewer PASS（Critical/High/Medium 0） | done | `7607e88` + records |
| ENG-BASE | FRAMEWORK-INTEGRATION-1 | 框架接入现有编排（GuardedOrchestrator 适配，2026-08-08，用户指令"继续开发，先不要全量门禁"）：guard_registration（manifest-checker 通过才注册，拒绝不触达 inner_register，inner 异常 fail-closed）、plan_to_chain（框架 Plan AGENT/HUMAN_GATE → 现有 OrchestrationChain：AGENT 按能力解析注册代理为 AGENT_CALL、HUMAN_GATE → HUMAN_CONFIRM、TOOL 节点与框架抽象能力明确拒绝 IntegrationError）、guarded_dispatch（validate_plan 前置 → plan_to_chain → Orchestrator.dispatch_event → 报告映射 OrchestrationOutcome：COMPLETED/HELD/ESCALATED，FAILED/未知 → ESCALATED fail-closed，内部异常仅类型名）、GuardResult 审计投影固定四键；纯函数 stdlib + L17（integration.md） | `src/coevo/framework/integration.py`; `src/coevo/framework/__init__.py`; `docs/framework/integration.md`; `docs/modules/framework.md` | `tests/unit/test_framework_integration.py`（11 项：混合节点/TOOL 拒绝/非 MVP 拒绝/无代理拒绝/无效 Plan 前置 REJECTED 不派发/三态映射/内部异常 ESCALATED/注册通过/拒绝不注册/注册内部异常 fail-closed/审计投影/stdlib 断言）+ 相邻框架回归 | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向 182/182 全绿；沙箱 fwint1-verify（pin=`def380e`）fmt/lint/182 全绿 + fwint1-sec STRIDE PASS（Critical/High/Medium 0，Low/Info 2 观察项已登记 DECISIONS），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `def380e` + 本轮 records |
| ENG-BASE | FRAMEWORK-INTEGRATION-2 | 存量链抬升与集成收口（2026-08-08，用户指令"继续开发，先不要全量门禁"）：chain_to_plan（现有 OrchestrationChain → 框架 Plan：AGENT_CALL 按 registry.get 解析能力 → AGENT 节点 / HUMAN_CONFIRM → HUMAN_GATE / CONDITIONAL 与未注册代理 → IntegrationError，顺序边 + plan_id=plan_fingerprint）、validate_product_chain（抬升 + validate_plan 五项不变量 + L18 + L19：RBAC 拒绝 fail-closed）、plan_to_chain 闭集外能力收敛为 IntegrationError（INTEGRATION-1 Low 收口，不泄漏 CapabilityValidationError 类型细节）；纯函数 stdlib + L17（integration.md） | `src/coevo/framework/integration.py`; `src/coevo/framework/__init__.py`; `docs/framework/integration.md`; `docs/modules/framework.md` | `tests/unit/test_framework_integration2.py`（6 项：混合链抬升/未注册代理拒绝/CONDITIONAL 拒绝/validate_product_chain 通过与 RBAC 拒绝/plan_to_chain 闭集外错误类型收敛/stdlib 断言）+ 相邻框架回归 | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向 188/188 全绿；沙箱 fwint2-verify（pin=`d80f7a1`）fmt/lint/188 全绿 + fwint2-sec STRIDE PASS（Critical/High/Medium 0，探针 7/7），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `d80f7a1` + 本轮 records |
| ENG-BASE | FRAMEWORK-INTEGRATION-3 | 真实产品接线（2026-08-08，用户指令"继续开发，先不要全量门禁"）：app/pipeline.py 在真实派发前用 validate_product_chain 校验 MVP_FIXED_CHAIN（框架门前置：RBAC/L4 结构 allow-all 留待产品接线，门失败抛 RuntimeError 中止）；chain_to_plan 抬升异常收敛为 rejected（IntegrationError → ValidationResult fail-closed）；L7 ISO 校验兼容小数秒（产品 now_utc_iso_z `...00.123456Z`，a2a/memory/validation/orchestrator 四模块统一）；纯函数 stdlib + L17 | `src/coevo/app/pipeline.py`; `src/coevo/framework/integration.py`; `src/coevo/framework/a2a.py`; `src/coevo/framework/memory.py`; `src/coevo/framework/orchestrator.py`; `src/coevo/framework/validation.py` | `tests/unit/test_pipeline_framework_gate.py`（2 项：MVP_FIXED_CHAIN 通过 + 未注册代理拒绝）; `tests/unit/test_framework_gaps2.py`（小数秒 ISO 用例）; `tests/e2e/test_demo_runner.py`（demo 管线 e2e 回归 3 项） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向 29/29 全绿；demo e2e 3/3（91.5s）；沙箱 int3-verify（pin=`e2c4cb1`）fmt/lint/29 全绿 + integ3_security STRIDE PASS（Critical/High/Medium 0，Low 1 已登记 DECISIONS），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `e2c4cb1` + 本轮 records |
| ENG-BASE | FRAMEWORK-INTEGRATION-4 | 注册门接入与 Manifest 构建器（2026-08-08，用户指令“继续开发，先不要全量门禁”）：framework/integration.py 新增 build_registration_manifest（纯函数生成规范 Agent Manifest，spec_hash 排除自指字段 metadata.spec_hash / policy_ref.spec_hash / policy_ref.signature，签名绑定 spec_hash|fingerprint，signer 可选注入）；app/demo_support.py 新增 DemoRegistrationVerifier / DemoRegistrationResolver / DemoRegistrationSigner / DemoPolicyRegistry（显式非生产，生产必须注入真实 SM2 验签与证书链，文档强警告）；app/pipeline.py 注册 4 个 demo 智能体前先 guard_registration（Manifest 结构 + 绑定格式 + policy 版本校验，失败抛 RuntimeError 不注册）；纯函数 stdlib + L17 | `src/coevo/framework/integration.py`; `src/coevo/app/demo_support.py`; `src/coevo/app/pipeline.py`; `docs/framework/integration.md`; `docs/modules/app.md`; `docs/modules/framework.md` | `tests/unit/test_framework_integration4.py`（6 项：builder spec_hash 一致 / 4 demo 全 accepted 且 inner_register 各一次 / 篡改拒绝 / 未知能力拒绝 / 缺 policy_version 拒绝 / stdlib 断言）+ 相邻框架回归（integration1/2、manifest_checker、capability、module_docs） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向 67/67 全绿；demo e2e 3/3；沙箱 fwint4b-verify（pin=`3d03490`）fmt/lint/67 全绿 + fwint4b-sec STRIDE PASS（Critical/High/Medium 0，Low 2 观察项登记 DECISIONS；安全套件主仓 99/99，沙箱 8 项环境性失败已核因），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `3d03490` + 本轮 records |
| ENG-BASE | FRAMEWORK-GAPS-4 | 共享 L7 校验构造器（2026-08-08，用户指令"继续开发，先不要全量门禁"）：validation.py 公开 is_iso_utc_z（小数秒 + 日历校验；正则 `$` 改 `\Z` 锚定拒尾部换行；非字符串 fail-closed 无 TypeError 泄漏）；a2a/memory/orchestrator/integration 统一引用去私有副本；validate_product_chain 异常分支也先过 L7（INTEGRATION-3 Low 收口）；包级导出；纯函数 stdlib + L17 | `src/coevo/framework/validation.py`; `src/coevo/framework/a2a.py`; `src/coevo/framework/memory.py`; `src/coevo/framework/orchestrator.py`; `src/coevo/framework/integration.py`; `src/coevo/framework/__init__.py` | `tests/unit/test_framework_gaps4.py`（包级导出 / 共享构造器正负例含小数秒尾部换行 / 非字符串 fail-closed / validate_product_chain 双分支 L7 拒绝）; 相邻回归 gaps2/a2a/memory/orchestrator/validate_plan/integration1/2/pipeline/agent_wire/module_docs | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向 85/85 全绿；沙箱 fwgaps4b-verify（pin=`ebc5ae4`）fmt/lint/85 全绿 + fwgaps4b-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，行为级探测全过），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `5088588` + `8b71456` + `ebc5ae4` + 本轮 records |
| ENG-BASE | FRAMEWORK-GAPS-5 | 全仓 ISO 正则尾部换行收口（2026-08-08，用户指令"继续开发，先不要全量门禁"；越权代理误记"全量门禁"已更正）：cockpit / crypto / knowledge_base / audit_governance / orchestrator / progress_capture / talent / task_decomposition 共 11 处 ISO 正则 `$` 改 `\Z`（消除 Python 末尾换行前匹配，与 GAPS-3/GAPS-4 同类），锚定回归测试 test_iso_anchor_regression.py；完整"统一到共享构造器"留作架构层后续 | `src/coevo/cockpit/models.py`; `src/coevo/cockpit/sessions.py`; `src/coevo/crypto/cng_handle.py`; `src/coevo/knowledge_base/models.py`; `src/coevo/audit_governance/models.py`; `src/coevo/orchestrator/models.py`; `src/coevo/progress_capture/models.py`; `src/coevo/progress_capture/watcher.py`; `src/coevo/talent/models.py`; `src/coevo/task_decomposition/agent.py`; `src/coevo/task_decomposition/baseline.py` | `tests/unit/test_iso_anchor_regression.py` | 编排者独立复核：锚定回归 443/443 全绿；增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；沙箱 fwgaps5-verify/fwgaps5-sec（pin=`88668af`）443/443 + STRIDE 探针 PASS（Critical/High/Medium/Low 0/0/0/0），check violations=[] 已 discard；全量 quality 按用户指示豁免（越权代理误跑两轮全量门禁因记录口径失败，详见 VERIFICATION） | done | `88668af` + records |
| ENG-BASE | FRAMEWORK-GAPS-6 | 共享 ISO 校验构造器全仓落地（2026-08-08，用户指令"继续开发，先不要全量门禁"）：新增依赖无关叶模块 src/coevo/timefmt.py（is_iso_utc_z：`\Z` 锚定拒尾部换行、小数秒、日历校验、非字符串 fail-closed），root_modules 登记；framework/validation.py 由 timefmt 导入并再导出；10 个产品模块 + 框架族统一引用共享构造器，去 11 处正则副本与包级 _ISO 再导出；锚定测试改测共享构造器 | `src/coevo/timefmt.py`; `src/coevo/framework/validation.py`; `src/coevo/cockpit/models.py`; `src/coevo/cockpit/sessions.py`; `src/coevo/crypto/cng_handle.py`; `src/coevo/knowledge_base/models.py`; `src/coevo/audit_governance/models.py`; `src/coevo/audit_governance/facade.py`; `src/coevo/orchestrator/models.py`; `src/coevo/orchestrator/service.py`; `src/coevo/orchestrator/_real_chain.py`; `src/coevo/progress_capture/models.py`; `src/coevo/progress_capture/watcher.py`; `src/coevo/talent/models.py`; `src/coevo/task_decomposition/agent.py`; `src/coevo/task_decomposition/baseline.py` | `tests/unit/test_framework_gaps6.py`; `tests/unit/test_iso_anchor_regression.py` | 编排者独立复核：增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向测试全绿；沙箱 fwgaps6b-verify/fwgaps6b-sec（pin=`ffafaf3`）fmt/lint + 定向测试 + STRIDE 探针 PASS（Critical/High/Medium/Low 0/0/0/0），check violations=[] 已 discard；全量 quality 按用户指示豁免（留待后续轮次） | done | `ffafaf3` + records |
| ENG-BASE | FRAMEWORK-DOCS-1 | 框架层文档治理收口（2026-08-08，用户指令"继续开发，先不要全量门禁"）：README 核心能力表加 US-16 行与框架层说明、架构树加 `framework/` + `timefmt.py`、文档索引加 `docs/framework/` 与 `docs/plans/distributed-agent-framework/`、当前状态加框架层 bullet；docs/code-guide.md 新增 framework/ 与 timefmt.py 引导节（模块职责 + 关键入口）；docs/README.md 索引登记 docs/framework/；新增文档治理守卫测试（README/code-guide/docs 索引覆盖断言 + docs/framework 文件存在断言） | `README.md`; `docs/README.md`; `docs/code-guide.md`; `docs/plans/FRAMEWORK-DOCS-1-slice.md` | `tests/unit/test_framework_docs.py`（4 项：README 覆盖 US-16/framework/timefmt/docs 索引 / docs 索引覆盖 framework / code-guide 引导节 / docs/framework 文件存在）; `tests/unit/test_module_docs.py`（4 项回归） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；定向测试 8/8 全绿；沙箱 fwdocs1-verify/fwdocs1-sec（pin=`398b6e9`）：fmt/lint/8 全绿 + STRIDE PASS（Critical/High/Medium/Low 0/0/0/0），check violations=[] 已 discard；全量 quality 按用户指示豁免 | done | `398b6e9` + 本轮 records |
| ENG-BASE | QUALITY-GATE-ENCODING-1 | 质量门禁输出编码修复（2026-08-08，用户指令“继续开发”）：quality_gate 子进程强制 UTF-8（gate_env：PYTHONIOENCODING=utf-8 + PYTHONUTF8=1，复制不污染父环境；两个 subprocess.run 均改用 gate_env），消除 VERIFICATION.md 门禁记录乱码根因（Windows 控制台 GBK 输出经 errors=replace 不可逆破坏）；历史乱码记录已清理（截除备份）；锁定脚本链同步（quality_gate.py 哈希 → python-script-lock.tsv → toolchain-lock script_inventory/source_sha256 → make.cs ScriptInventorySha256）；补回归测试；仅 stdlib 离线 | `scripts/quality_gate.py`; `docs/dependencies/python-script-lock.tsv`; `docs/dependencies/toolchain-lock.json`; `scripts/tool-shims/make.cs`; `tests/unit/test_quality_gate_encoding.py` | `tests/unit/test_quality_gate_encoding.py`（3 项：env 复制语义 / 中文回传无替换符 / 全部 subprocess 用 gate_env）；`tests/security/test_local_toolchain_security.py` + `tests/integration/test_dev_environment_entry.py`（锁链一致回归） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed，输出无乱码）；单元全量 1175 项 + 安全套件 99 项全绿；沙箱 fwenc2-verify/fwenc2-sec（pin=`8576013`）fmt/lint/定向全绿 + STRIDE PASS，check 均 violations=[]；全量 quality 按用户指示豁免 | done | `8576013` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-1 | 基于框架优化原应用实现（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，不做全量门禁"）：AgentRegistry.by_capability 惰性能力索引（O(V)→摊销 O(1)，注册顺序语义不变，register/set_status 新实例经 __post_init__ 自动失效）；build_registration_manifest 去除 json 双重序列化（结构化剥离自指字段，wire 字节不变并字节级回归锁定 sha256=00ff9ada…）；chain_to_plan 用 dataclasses.replace 一次成型 plan_id（节点/边冻结复用）；demo 注册装配收敛到 demo_support.register_demo_agents（显式非生产，仍强制 guard_registration）+ pipeline 组合根复用模块级 allow-all RBAC/scope 检查器；纯函数 stdlib + L17 | `src/coevo/orchestrator/models.py`; `src/coevo/framework/integration.py`; `src/coevo/app/demo_support.py`; `src/coevo/app/pipeline.py` | `tests/unit/test_framework_optimize1.py`（5 项：by_capability 顺序/缓存失效/不可变、manifest 字节回归、chain_to_plan 指纹与结构、demo 装配 4 代理）；回归 integration2/integration4/pipeline_gate/integration/orchestrator/module_docs | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1180 项 + 安全套件 99 项全绿；沙箱 fwopt1-verify（pin=`8777542`）fmt/lint/定向 60 全绿 + fwopt1-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，变更面仅 4 源码 + 新测试、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `8777542` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-2 | 共享时间生成器全仓落地（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，不做全量门禁"）：timefmt 新增 now_utc_iso_z（依赖无关叶子模块，格式与既有副本完全一致含微秒与尾随 Z），收敛 13 处私有副本（knowledge_base/audit_governance/cockpit/demo_support/task_flow/protocol×4/progress_capture/task_decomposition/talent/crypto）与 3 处直接内联（identity×3）；公开导出名（cockpit.sessions/demo_support）保留 API 仅改来源；行为与 wire 字节不变；全仓源码守卫防回归；stdlib only | `src/coevo/timefmt.py`; `src/coevo/app/demo_support.py`; `src/coevo/audit_governance/stream_store.py`; `src/coevo/cockpit/sessions.py`; `src/coevo/crypto/cng_handle.py`; `src/coevo/identity/audit_anchor.py`; `src/coevo/identity/private_keys.py`; `src/coevo/identity/repository.py`; `src/coevo/knowledge_base/store.py`; `src/coevo/progress_capture/watcher.py`; `src/coevo/protocol/import_service.py`; `src/coevo/protocol/package_store_db.py`; `src/coevo/protocol/sm2_keywrap.py`; `src/coevo/protocol/sm2_sign.py`; `src/coevo/talent/store.py`; `src/coevo/task_decomposition/baseline.py`; `src/coevo/task_flow/parser.py` | `tests/unit/test_framework_optimize2.py`（2 项：now 输出合法含微秒 / 全仓源码守卫——timefmt 唯一 def、无内联副本） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1182 项 + 关键集成 72 项 + 安全套件 99 项全绿；沙箱 fwopt2-verify（pin=`db0a550`）fmt/lint/定向 87 全绿 + fwopt2-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，无导入环、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `db0a550` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-3 | 共享 canonical JSON 序列化与摘要（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，不做全量门禁"）：新增根级叶子 src/coevo/canon.py（canonical_json_bytes / canonical_digest，stdlib only），收敛框架内部重复（integration._canonical / manifest_checker._canonical_bytes 字节语义相同）与 identity 5 处 digest 内联（repository 事件哈希链 2 + business digest 1、validation bundle digest 1、private_keys 审计链 1）；ensure_ascii 语义逐点保留（business digest=False）、digest 逐位不变；root_modules.md 登记；全仓源码守卫防回归 | `src/coevo/canon.py`; `src/coevo/framework/integration.py`; `src/coevo/framework/manifest_checker.py`; `src/coevo/identity/repository.py`; `src/coevo/identity/validation.py`; `src/coevo/identity/private_keys.py`; `docs/modules/root_modules.md` | `tests/unit/test_framework_optimize3.py`（6 项：canonical 字节/哈希回归含 ensure_ascii 两分支、框架无私有 canonical、identity 无 digest 内联、root_modules 登记、事件哈希可复现） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1188 项 + 安全套件 99 项全绿；沙箱 fwopt3-verify（pin=`bc44f87`）fmt/lint/定向 71 全绿 + fwopt3-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，审计/私钥链 digest 逐位不变、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `bc44f87` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-4 | 框架默认策略 Profile 惰性缓存（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，不做全量门禁"）：default_profiles() 一次性构造并缓存 4 个默认 Policy（Policy 与嵌套 Profile 全 frozen 不可变，安全共享），get_default_profile 字典 O(1) 查找且 fail-closed 保留（未知名仍抛 PolicyValidationError）；消除 pipeline/validate_plan 等消费点每次重复构造 Policy（O(4)×N → 一次构造 + O(1)）；docs/modules/framework.md 同步；stdlib only | `src/coevo/framework/policy.py`; `docs/modules/framework.md` | `tests/unit/test_framework_optimize4.py`（4 项：缓存同一对象且 frozen 不可变、get_default_profile 缓存命中、未知名 fail-closed、默认数值边界不变） | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1192 项 + 安全套件 99 项全绿；沙箱 fwopt4-verify（pin=`e3e27a9`）fmt/lint/定向 68 全绿 + fwopt4-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，缓存共享不可变语义确认、产品代码无 Policy 篡改、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `e3e27a9` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-5 | real_chain_store 收敛到共享 canonical（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，做全量门禁，成功后 push 到 github"）：canon.py 的 canonical_json_bytes/canonical_digest 新增 allow_nan 参数（默认 False 拒绝 NaN/Infinity，fail-closed）；orchestrator/real_chain_store 的 canonical_json_bytes 保留严格类型校验（非有限 float/非 JSON 拒绝、RealChainStoreError 语义）但序列化委托共享 canon（ensure_ascii=False/allow_nan=False，字节逐位不变），canonical_digest 经共享序列化计算摘要；root_modules.md 补充 allow_nan 语义 | `src/coevo/canon.py`; `src/coevo/orchestrator/real_chain_store.py`; `docs/modules/root_modules.md` | `tests/unit/test_framework_optimize5.py`（6 项：canon NaN 默认拒绝/显式允许、store 字节与共享 canon 一致、非有限 float 与非 JSON fail-closed、无内联序列化守卫） | **全量门禁** fmt/lint/test(unit+integration+go)/test-security/test-e2e exit=0 fingerprint=`34d637f035600903`（audit fully-sealed）；单元全量 1198 项 + 安全套件 99 项全绿；沙箱 fwopt5-verify（pin=`50e7ef4`）fmt/lint/定向 56 全绿 + fwopt5-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，严格校验保留、字节逐位不变、tests/security 零改动），check 均 violations=[] | done | `50e7ef4` + 本轮 records + push |
| ENG-BASE | FRAMEWORK-OPTIMIZE-6 | demo 组合根阶段化收敛（2026-08-08，用户指令"基于框架，优化原来系统应用的代码实现，包括数据结构、算法与模块架构，不做全量门禁"）：run_demo_pipeline（~250 行大函数）的包导出/驾驶舱快照/知识库入库/审计流 4 段内联提取为模块级阶段函数（_export_demo_package/_build_demo_cockpit_views/_store_demo_knowledge/_publish_demo_audit，均标注 DEMO-ONLY），组合根薄编排、行为不变（demo e2e 3/3 回归）；hashlib/json import 收敛到包导出阶段；阶段函数可独立单测 + 架构守卫（内联体仅存在于各自助手内） | `src/coevo/app/pipeline.py` | `tests/unit/test_framework_optimize6.py`（4 项：cockpit 快照字段、审计流 3 事件、知识库入库建库、内联体守卫）；回归 test_pipeline_framework_gate + demo e2e 3/3 | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1202 项 + 安全套件 99 项全绿；demo e2e 3/3；沙箱 fwopt6-verify（pin=`1fd6908`）fmt/lint/定向 6 全绿 + fwopt6-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，demo-only 边界与 fail-closed 保留、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `1fd6908` + 本轮 records |
| ENG-BASE | FRAMEWORK-OPTIMIZE-7 | 真实链失败收尾路径去重（2026-08-08，用户指令"继续"，延续"基于框架，优化原来系统应用的代码实现……不做全量门禁"）：orchestrator/_real_chain.py 的 dispatch_real_chain 中 3 处结构相同的失败收尾（agent 不可用 / facade 失败 / facade 重试失败：追加 ESCALATED trace + _finish_dispatch_terminal）提取为 _escalate_and_finish 单一辅助，行为不变（ESCALATED 状态与审计存储语义逐位一致）；守卫测试钉住 3 个失败 detail 各单一调用点 | `src/coevo/orchestrator/_real_chain.py` | `tests/unit/test_framework_optimize7.py`（2 项：_escalate_and_finish 行为回归含真实 RealChainStore、失败 detail 单一调用点守卫）；回归 test_orchestrator + test_orchestrator_real_facade_chain + demo e2e 3/3 | 增量门禁 fmt exit=0 fingerprint=`fe39766e2048d2bc`；lint exit=0 fingerprint=`252ad24e526f6728`（audit fully-sealed）；单元全量 1204 项 + 安全套件 99 项全绿；demo e2e 3/3；沙箱 fwopt7-verify（pin=`435eff6`）fmt/lint/定向 28 全绿 + fwopt7-sec STRIDE PASS（Critical/High/Medium/Low 0/0/0/0，ESCALATED 语义与 fail-closed 保留、tests/security 零改动），check 均 violations=[]；全量 quality 按用户指示豁免 | done | `435eff6` + 本轮 records |
