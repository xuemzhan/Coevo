# Coevo

分布式任务管理具身智能体系统 MVP。需求基线位于 `docs/`，工程循环状态位于 `loop/`。

Windows 离线开发入口：

```powershell
.\scripts\dev.ps1 -Task quality
```

完整环境与离线依赖规则见 `docs/development-environment.md`。工程底座已完成；当前产品开发从 US-0 身份与信任能力开始，并严格遵循单工作项、独立验证和独立安全审查门禁。
